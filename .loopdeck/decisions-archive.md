# Decisions Archive

## 2026-07-26 — Bound Codex initialization, not active-turn silence

- **Status**: accepted
- **Context**: The Codex app-server adapter applied a 300-second timeout to every stdout read. A healthy agent can emit no JSONL while a long-running tool executes, so the adapter falsely reported “Codex produced no stdout … assuming stuck”; the generic error reconciliation then appended a misleading `process_exited` interruption marker.
- **Consequences**: The 300-second bound now applies only while waiting for initialization, thread start/resume, and turn-acceptance responses. Once a turn is accepted, stdout reads are unbounded because protocol silence is not proof of a hang; EOF still detects an exited child, and the existing interrupt channel keeps Stop responsive. This prevents the false timeout and its derived interruption marker without allowing a broken handshake to wait forever.

## 2026-07-26 — Coalesce Codex stream deltas at persistence and render boundaries

- **Status**: accepted
- **Context**: Codex app-server emits assistant text in token-sized deltas. Live state coalesced adjacent deltas, but `CodexSession` persisted every delta as a separate `ContentBlockRecord`; after reload, `BlockList` rendered each record through a block-level Markdown component, producing one word or token per line.
- **Consequences**: The Codex adapter now merges adjacent text and adjacent thinking deltas before persistence, while tool calls and content-type changes remain ordering boundaries. The frontend also coalesces adjacent prose blocks before rendering, repairing existing fragmented transcripts in memory without rewriting historical JSONL files. New transcripts stay compact and old transcripts display correctly.

## 2026-06-22 — Use Tauri v2 for desktop shell
- **Status**: accepted
- **Context**: We needed a cross-platform desktop shell for LoopDeck. Electron was the default, but bundle size and the need for a performant filesystem scanner pushed us to evaluate alternatives.
- **Consequences**: Must maintain Rust + TypeScript across the IPC boundary. No Node.js main process — all system operations go through Tauri commands. Smaller bundle, better FS performance.

## 2026-06-22 — Use Zustand for frontend state management
- **Status**: accepted
- **Context**: React Context + useReducer was verbose for cross-component state. Redux added too much boilerplate. Zustand offered minimal API with selector-based subscriptions and no provider wrapping.
- **Consequences**: All state lives in `src/store/appStore.ts`. Components subscribe via selectors. No Redux DevTools dependency (though Zustand supports it optionally).

## 2026-06-22 — Store project memory inside repos, not a database
- **Status**: accepted
- **Context**: Other AI project managers (Codex, Cursor rules) store context in proprietary formats or cloud databases. LoopDeck needed to be local-first and repo-portable so context travels with the code.
- **Consequences**: `.loopdeck/` directory lives in each repo. Global registry at `~/.config/loopdeck/config.yaml` is an index, not source of truth. Files are Markdown + YAML for human + AI readability.

## 2026-06-22 — Markdown for agent memory files (decisions.md, loops.md)
- **Status**: accepted
- **Context**: For V2 agent memory, we considered YAML, JSON, and SQLite. Markdown was chosen because it's human-readable, AI-friendly (all LLMs understand Markdown), and doesn't require a parser library on the agent side.
- **Consequences**: The Rust backend uses regex-free line-by-line Markdown parsing. Loosely structured — the conventions (## headings, **Key**: Value bullets) are enforced by convention, not schema validation.

## 2026-06-22 — Sidebar tab navigation in ProjectDetail instead of single scroll view
- **Status**: accepted
- **Context**: V2 adds Decisions and Loops tabs alongside the existing Overview. A single scroll view would be too long. Tabs allow clear separation of concerns.
- **Consequences**: ProjectDetail now has a left sidebar (180px) with tab buttons and a right content panel. Overview becomes one of three tabs.

## 2026-06-22 — Dual Stop hooks for agent memory auto-write
- **Status**: superseded
- **Context**: We need AI agents to update .loopdeck/ files automatically at session end. Two approaches: prompt-based (AI writes rich content) and shell script (mechanical fallback). Both are complementary.
- **Consequences**: `.claude/settings.local.json` has a Stop hook with both a prompt and a shell command. The prompt gives richer content; the shell script ensures the files always exist.

## 2026-06-22 — Stop hook dirty-flag gating (temporary workaround)
- **Status**: accepted
- **Context**: The Stop hook was firing on every session end — even pure Q&A with no code changes — causing repetitive "LOOPDECK MEMORY UPDATE REQUIRED" nags. Added a PreToolUse hook that creates `.claude/.session-dirty` on Edit/Write, and the Stop hook only emits the reminder when that flag exists.
- **Consequences**: Cleaner sessions — no nag on Q&A. But this is a **temporary workaround**. The real fix is to move all agent control (CLAUDE.md, skills, hooks, memory conventions) into the LoopDeck app itself, where it can intelligently decide when to prompt. See parking lot in loops.md.

## 2026-06-23 — Moved LoopDeck memory hooks to global config with .loopdeck/ gating
- **Status**: accepted
- **Context**: The memory-update hooks (PreToolUse dirty flag + Stop reminder/heartbeat) were project-local to the loopdeck repo. Any LoopDeck-tracked project with a `.loopdeck/` directory should get the same memory-update automation.
- **Consequences**: Scripts live at `~/.claude/hooks/loopdeck-stop-hook.py` and `loopdeck-memory-write.sh`. Both gate on `.loopdeck/` directory existence — in non-LoopDeck projects they exit silently. Hooks configured in global `~/.claude/settings.json`. Project-local `settings.local.json` cleaned up (hooks removed, permissions kept for dev use).

## 2026-06-23 — Fixed Stop hook matcher and dirty-flag race condition
- **Status**: accepted
- **Context**: The Stop hooks were configured with `"matcher": ""` which only matches an empty string — it never matches real stop reasons like `"finished"` or `"interrupted"`. Additionally, the Python reminder hook consumed `.session-dirty` (deleted it), starving the shell heartbeat fallback. Both issues meant the memory-update reminder never fired.
- **Consequences**: Changed matcher to `".*"` (match any stop reason). Python script now reads `.session-dirty` without deleting it; only the shell fallback consumes the flag. Both Stop hooks can now see the dirty flag and both can fire.

## 2026-06-23 — Git-age-based project status auto-classification
- **Status**: accepted
- **Context**: We needed project status (Active/Warning/NonActive) to reflect reality, not manual user input. The `rescan_project` command already refreshes git info — it's the natural place to derive status.
- **Consequences**: Status is derived from last commit date during rescan: 0–6 days → Active, 7–30 days → Warning, 30+ days → NonActive. Fallback to last_modified if no commits. Archived remains a manual status. Frontend color-codes status badges (green/yellow/red/gray). Imported projects default to Active until first rescan.

## 2026-06-22 — Stop hook must use command type, not prompt type
- **Status**: accepted
- **Context**: The initial Stop hook used `type: "prompt"` to remind the AI to update .loopdeck/ files. This silently failed because Claude Code's hook system only allows `type: "prompt"` on PreToolUse, PostToolUse, and PermissionRequest events — NOT on Stop. Stop only supports `type: "command"`.
- **Consequences**: Replaced the prompt hook with a `type: "command"` hook that runs python3 to output JSON with `hookSpecificOutput.additionalContext`. This injects the memory update reminder into the model's context on next wake-up. The shell script fallback (Approach B) was unchanged — it was already a valid command hook. Both hooks now fire correctly on Stop.

## 2026-06-24 — Adopt Tailwind CSS v4 + OKLCH dark palette from AI Project Command
- **Status**: accepted
- **Context**: LoopDeck's UI used plain CSS with BEM naming and a GitHub-dark hex palette. The AI Project Command app (reference UI) uses a more polished design: Tailwind CSS v4, OKLCH color space, layered surface backgrounds, gradient accent lines, and Inter+JetBrains Mono fonts. Adopting this design system makes LoopDeck look professional and consistent.
- **Consequences**: All component CSS files deleted — styles now live in Tailwind utility classes. Single `src/styles.css` with OKLCH tokens and Tailwind config. AppShell changed from top header bar to sidebar navigation layout. Added PageHeader component for sticky view headers. Design tokens (colors, radii, fonts, shadows) match AI Project Command exactly.

## 2026-07-03 — Per-content-block streaming granularity for `ClaudeEvent`

- **Status**: accepted
- **Context**: The `--output-format stream-json` stream produces one NDJSON line per `assistant` message, and each message can contain multiple `content` blocks (text, thinking, tool_use). We had a choice: emit one `ClaudeEvent` per NDJSON line (the entire message), or break messages apart and emit one event per content block.
- **Consequences**: Chose per-content-block emission (`TextDelta`, `ThinkingDelta` per block). This gives the frontend the most natural granularity — each delta is a complete text fragment ready to render. The accumulator (`ResponseAccumulator`) still processes the full message in one `ingest_event` call, keeping aggregation consistent with the batch path.

## 2026-07-03 — Separate batch and streaming `send_message` paths (not refactored into one)

- **Status**: accepted
- **Context**: After adding `send_message_streaming`, we considered refactoring `send_message` to call `send_message_streaming` with a no-op or dropped channel, so the read loop lives in one place. The two paths share `ResponseAccumulator` but differ meaningfully: the batch path is simpler (just accumulate), while the streaming path adds per-block event emission and a terminal `Result` event.
- **Consequences**: Kept them separate (~15 lines of stdin-write duplication). The batch path avoids allocating channel overhead and remains the simpler, easier-to-reason-about code path. The streaming path adds exactly the delta-emission logic and nothing else. If a third variant is ever needed, extraction becomes worthwhile — for two, the duplication is cheaper than the abstraction.

## 2026-07-03 — Best-effort channel sends in streaming (closed channel is not an error)

- **Status**: accepted
- **Context**: The frontend may close the Tauri `Channel<ClaudeEvent>` mid-turn (e.g., navigating away from the Agent tab). The Rust side could either (a) treat a closed channel as fatal and abort the turn, or (b) silently drop the send and let the turn complete.
- **Consequences**: Chose (b) — best-effort sends with `let _ = channel.send(…)`. Rationale: the transcript is always recorded regardless (it's written after the turn completes in `send_and_record_streaming`), and aborting mid-turn would orphan a running claude process with an incomplete stdin stream. The user can always see the result in the transcript when they return.

## 2026-07-19 — Stop hook re-injection caused runaway token usage (~30M/hr)

- **Status**: accepted
- **Context**: The LoopDeck memory-update Stop hook (`loopdeck-stop-hook.py`) emitted `hookSpecificOutput.additionalContext` instructing the model to *read* `.loopdeck/loops.md` and `.loopdeck/decisions.md` after every turn. Stop fires after every agent turn (not just session end), so the instruction re-injected every wake-up. With `loops.md` grown to 101 KB (~25K tokens) and `decisions.md` to 50 KB (~12K tokens), each turn paid ~38K tokens just to re-read memory, and the files kept growing because nothing pruned them. Observed ~30M tokens consumed in under an hour. Two compounding bugs: (1) the dirty-flag *creator* (a `PreToolUse` hook on Edit/Write) was missing from the config, so the gating that should have suppressed the nudge on read-only turns was broken across harnesses; (2) the hook config was duplicated across `.claude/settings.json` and `.Codex/hooks.json`, risking double-fire.
- **Consequences**: Redesigned the hooks to be append-only and gated. (1) `loopdeck-stop-hook.py` now emits a ~3-line nudge that asks for an APPEND only — it never says "read loops.md." (2) Added the missing `loopdeck-dirty-flag.py` (PreToolUse on `Edit|Write|MultiEdit|NotebookEdit`) that creates `.claude/.session-dirty`; the Stop nudge and `loopdeck-memory-write.sh` both gate on it, so Q&A and read-only turns cost zero. (3) Archived `loops.md` History (1541 lines / 90 KB) to `loops-archive.md` with a pointer — live file is now 118 lines / 11 KB (−89%). (4) Removed 5 duplicate "Session heartbeat" noise entries from `decisions.md`. (5) Synced `.Codex/hooks.json` to match `.claude/settings.json`. Per-turn memory cost dropped from ~38K tokens to ~3 lines. The deeper lesson, already in the Parking Lot: agent control (CLAUDE.md, skills, hooks, memory conventions) should move into the LoopDeck app itself so it can decide intelligently when to prompt, rather than relying on external hook files that re-inject context every turn.

## 2026-07-03 — Streaming command returns `()` not `AgentResponse`

- **Status**: accepted
- **Context**: `agent_send_message` returns `AgentResponse` as the Tauri command return value, which the frontend awaits as a Promise. For streaming, the frontend gets data through the Channel, not the return value. We could return `AgentResponse` as well (giving two sources of truth), or return `()`.
- **Consequences**: Chose `()` — the terminal `ClaudeEvent::Result` carries the full aggregated response inline as the last event on the channel. This is a single source of truth: the frontend listens to the channel and uses the `Result` event to finalize its UI state. Returning `AgentResponse` as well would create ambiguity about which payload to trust.

## 2026-07-03 — Frontend streaming chat UI uses Channel events as single source of truth

- **Status**: accepted
- **Context**: After adding `agent_send_message_streaming` (Rust) and `ClaudeEvent` types (TS), the `AgentPanel` still used batch APIs exclusively — users saw a spinner for the full turn duration. Building a streaming UI required deciding how the frontend detects turn completion, errors, and final state.
- **Consequences**: The Tauri `Channel<ClaudeEvent>` is the single source of truth for turn state. The `invoke` Promise is only used for infra-level error catching (timeout, no config, spawn failure). Model-level errors (`is_error: true`) are surfaced from the `ClaudeEvent::Result` event, not from Promise rejection. This is consistent with the existing decision that streaming commands return `()` not `AgentResponse`. The `StreamingBubble` component accumulates `TextDelta`/`ThinkingDelta` events in real time and transitions to a "complete" state when the terminal `Result` event arrives. A `mountedRef` guard prevents post-unmount state updates; a `resultHandled` flag prevents double-reload if the invoke Promise resolves before the last channel event.

## 2026-07-03 — Streaming "Start next loop" variant added for UI consistency

- **Status**: accepted
- **Context**: The existing `agent_start_loop` was batch-only. Adding streaming to the free-form send but not to Start-next-loop would create an inconsistent UX — the first turn of every session would show a spinner while follow-ups streamed. We could either (a) have the frontend build the prompt and call `agent_send_message_streaming`, or (b) add `agent_start_loop_streaming` in Rust.
- **Consequences**: Chose (b) — `agent_start_loop_streaming` mirrors the batch `agent_start_loop` exactly (same prompt-building logic via `build_next_loop_prompt`, same transcript recording via `send_and_record_streaming`) and differs only in how the response reaches the UI. This keeps prompt-building logic in one place (Rust) and avoids duplicating `build_next_loop_prompt` / `next_unchecked_loop_step` on the frontend. The two streaming commands (`agent_start_loop_streaming` and `agent_send_message_streaming`) share the same `send_and_record_streaming` pipeline.

## 2026-07-03 — Extracted presentational `Chat` component from `AgentPanel`

- **Status**: accepted
- **Context**: After the streaming AgentPanel was built, all rendering logic (`TurnBubble`, `StreamingBubble`, `ThinkingBlock`, composer, error banner, empty state) was inline in AgentPanel alongside Channel lifecycle management — the file mixed orchestration and presentation concerns. For reusability (future `/agent` standalone view, potential mobile port) and testability, rendering needed its own module.
- **Consequences**: `Chat.tsx` is a pure presentational component with zero Tauri or Channel imports. All streaming state (`streamingText`, `streamingThinking`, `streamingResult`, `busy`, `error`) flows in via props; user actions flow out via callbacks (`onSend`, `onClearError`). `AgentPanel.tsx` retains sole ownership of the `Channel<ClaudeEvent>` lifecycle, delta accumulation, transcript persistence (`reload()`), and toolbar buttons. This one-way data flow makes `Chat` independently reusable and testable, while `AgentPanel` remains the single source of truth for streaming orchestration. The `streamingResult` prop gives `Chat` enough context to show usage/duration meta in the transient window before the transcript reload replaces the streaming bubble with the persisted turn.

## 2026-07-03 — Agent Runner uses terminal theme, not Chat bubbles

- **Status**: accepted
- **Context**: The Agent Runner view needed a standalone agent interface with a project selector. We considered reusing the `Chat` component (which already handles streaming rendering) but `Chat`'s bubble-based, avatar-heavy design doesn't fit a "terminal" aesthetic. The Agent Runner is meant to feel like a developer tool — a tmux-for-AI-agents — not a chat app.
- **Consequences**: `AgentRunner.tsx` renders its own terminal-themed output (monospace font, dark background `oklch(0.13 0.01 270)`, prompt indicators `❯`, flat line-by-line output, tool-call diamonds `◈` in warning color). It does NOT import `Chat`. However, it does reuse the same streaming orchestration pattern as `AgentPanel` — Channel → `onmessage` → delta accumulation → Result → reload. The two components are rendering-siblings but orchestration-cousins: same approach to Channel lifecycle, different visual output. If a third agent view is ever needed, the shared streaming logic should be extracted into a `useStreamingTurn` hook.

## 2026-07-03 — Activity Feed merges all sources into single chronological timeline

- **Status**: accepted
- **Context**: The Activity Feed needs to show agent turns, decisions, and loop completions across all projects. We considered rendering separate sections per data source (Conversations, Decisions, Loops) within the feed, vs merging everything into one sorted timeline.
- **Consequences**: Chose a single merged `ActivityEvent[]` discriminated by `kind` (`turn_user` | `turn_assistant` | `turn_error` | `decision` | `loop_completed`). One sort pass, one date-grouping pass, one render loop. The alternative (separate sections) would fragment chronology and require users to mentally merge timelines. Date-only sources (decisions, loops) get synthesised midnight UTC timestamps so they sort within the correct date bucket. Per-source fetching is best-effort — a missing transcript doesn't prevent decisions/loops from appearing.

## 2026-07-03 — Standalone Decisions + Loops use expand-in-place cards, not detail pages

- **Status**: accepted
- **Context**: Both the standalone Decisions and Loops views aggregate data across all projects. We could either (a) use click-to-expand cards showing detail inline, or (b) navigate to a per-item detail page (requiring selected-decision-ID / selected-loop-path in Zustand state). With TanStack Router not yet adopted, option (b) would add routing-like state management prematurely.
- **Consequences**: Chose expand-in-place cards for both views. Clicking a decision card expands it inline to show full context + consequences. Clicking a loop card expands it to show next-steps checklist + history. The ChevronDown icon rotates 180° to indicate state. This keeps the list visible for context while showing detail, and defers the routing complexity until TanStack Router is adopted.

## 2026-07-03 — TanStack Router: memory history, Zustand for data only

- **Status**: accepted
- **Context**: The app used a Zustand `currentView` string + conditional render (`{currentView === "dashboard" && <Dashboard />}`) for view switching. This worked but had no URL-based routing, no type-safe params, and no nested layout support. We needed proper client-side routing before adding more views.
- **Consequences**: Adopted `@tanstack/react-router` v1 with `createMemoryHistory` (no browser URL bar in Tauri). Routes: `/`, `/activity`, `/agent`, `/decisions`, `/loops`, `/settings`, `/import`, `/project/$projectPath`. The root route is a layout component (`AppShellLayout`) with sidebar + `<Outlet />`. Zustand retains data state (`projects`, `selectedProject`, `detailTab`, `pendingAgentStart`) but no longer owns the current view. Project filesystem paths are URI-encoded in the route param (`encodeURIComponent`/`decodeURIComponent`). Navigation uses `<Link>` (sidebar) and `useNavigate()` (programmatic). The persisted Zustand slice was reduced from `{currentView, selectedProject, detailTab}` to `{selectedProject, detailTab}` — the router's memory history handles the current location.

## 2026-07-08 — Spec layer in `docs/epics/`, runtime layer in `.loopdeck/`

- **Status**: accepted
- **Context**: LoopDeck executed work as a flat list of loops in `.loopdeck/loops.md`, mixing plan + execution in one file. Planning past ~10 items broke down — no grouping, no ownership boundary, no reviewable spec. The original proposal put epics in `.loopdeck/epics.md`, but that would conflate the plan (intention, authored deliberately) with runtime state (what the app writes during execution) and treat the plan as mutable app state that drifts.
- **Consequences**: Epics and PRDs live under `docs/epics/<slug>/` (one directory per epic, co-located PRDs), committed to git and reviewable in PRs. `.loopdeck/` stays the runtime layer — current loop, history, decisions. The bridge is a single promote-to-loop action that writes a PRD checklist item into `loops.md ## Current` carrying `**Epic**`/`**PRD**` back-references. The agent stays unaware of the hierarchy (no change to `build_next_loop_prompt`); the app owns the spec layer, the human owns the commit. Two views of the same work (PRD checklist vs. History) will drift — 0.2.0 makes that drift visible rather than auto-syncing. Full spec: `docs/epics/support-project-management/`.

## 2026-07-08 — YAML frontmatter for spec files, bullets for runtime files

- **Status**: accepted
- **Context**: The first epic format draft used `**Milestone**: 0.2.0` bullets in the body, mirroring `decisions.md`/`loops.md`. But epics need to be *indexed* — grouped by milestone, filtered by status — which is structurally the same job SKILL.md solves with YAML frontmatter (a small structured header for discovery, prose body for content). Bullets conflate the index layer with the content layer and force the parser to line-scan for fields that should be queryable.
- **Consequences**: Spec-layer files (`docs/epics/**/*.md`) carry YAML frontmatter with index fields (`title`, `slug`, `milestone`, `status`, `started`, `completed`, `owner`, `description`); the body is pure prose. Runtime-layer files (`.loopdeck/*.md`) keep the `**Field**: value` bullet convention — they're agent-written and lenient. The `/epics` view groups by `milestone` via a frontmatter query, no body parsing. The format difference reinforces the layer separation: structured where humans index, lenient where agents write. The back-reference the app writes into `loops.md` on promote stays as bullets, because it's writing into a runtime file. `epic.rs` uses `serde_yaml` (already in the dep tree) for frontmatter and line-scans only the `### Phase` sections. `milestone` is quoted (`"0.2.0"`) because YAML parses unquoted `0.2.0` as the float `0.2`.

## 2026-07-08 — Split `loopdeck-orchestrator` into runner + author + memory

- **Status**: accepted
- **Context**: The single `loopdeck-orchestrator` skill conflated runtime mechanics (how to execute a loop) with strategy (how to plan a project). Once the spec layer lives in `docs/epics/`, the strategy content collides with the app-owned plan — the agent would keep re-decomposing plans the human already authored. The skill needed stripping to mechanics.
- **Consequences**: Three focused skills replace one: `loopdeck-loop-runner` (mechanics + a new read-context rule that follows a loop's epic/prd back-reference as context, not mandate), `loopdeck-epic-author` (human-invoked drafting aid that elaborates a coarse goal via clarifying questions into reviewable drafts — never commits or promotes), `loopdeck-memory` (decisions.md + loops.md write conventions). Authoring intelligence moves to app-invoked dialogues in 0.3.0, not standing skills. Skill descriptions must be phrased on-demand ("when the user wants to draft") to avoid recreating autonomous-planning drift.

## 2026-07-08 — Managed-skills refresh via version manifest

- **Status**: accepted
- **Context**: `copy_skills` skips any skill whose `SKILL.md` already exists, to preserve user customizations. This made the orchestrator split unreachable on existing projects — they'd keep the fat self-directing version forever. Real fix is runtime skill injection (Parking Lot: "Move agent control into LoopDeck app"), but that shouldn't gate 0.2.0.
- **Consequences**: Stopgap: a `.claude/skills/.loopdeck-manifest.json` records installed skills + app version. The `loopdeck-` prefix is the ownership boundary — app-managed skills are overwritten when app version advances; user-owned skills (no prefix) are never touched. A one-time migration removes the legacy `loopdeck-orchestrator` directory, logged. Users customize by copying to a non-prefixed name. The convention carries forward to the runtime-injection model when it lands.

## 2026-07-10 — Auth token stored in OS keychain, not config.yaml

- **Status**: accepted
- **Context**: The agent auth token (an API key for the model provider) was stored in plaintext at `~/.config/loopdeck/config.yaml`. The threat model isn't a remote attacker — it's local exposure: the file gets swept into backups, cloud-sync folders, accidental commits, or read by another local process on a shared machine. The OS credential store (macOS Keychain / Windows Credential Manager / Linux Secret Service) provides OS-managed encryption and per-item access control for free, and is the standard place desktop apps keep secrets. `chmod 600` on the file was the interim floor already noted in the audit, but it doesn't help against backups/sync and only slows a same-user process.
- **Consequences**: The token now lives in the keychain via the `keyring` crate (`secrets.rs`); it is never written to `config.yaml` — only `base_url` / `model` / `effort` are. `get_agent_config` never returns the plaintext token to the renderer; it returns a `has_auth_token` presence flag so the Settings UI can show a masked "token stored" affordance. The token is resolved from the keychain at spawn time into a local `AgentConfig` (via `resolve_agent_config`) and set as the child's `ANTHROPIC_AUTH_TOKEN` env var — it is not held on the long-lived `Mutex<GlobalConfig>`. A one-time startup migration (`migrate_auth_token_to_keychain`) moves any existing plaintext token to the keychain and scrubs the file. `config.yaml` is still tightened to `0600` on every save as defense-in-depth, and if the keychain is unavailable (e.g. headless Linux with no D-Bus) the token stays in the `0600` file as the interim floor rather than being dropped. A new `clear_auth_token` command + Settings button lets the user revoke a stored token. The `AgentConfig.auth_token` field is retained on the wire type (the frontend sends a new token through it on save) but is always `None` on read.

## 2026-07-10 — Offload blocking I/O in Tauri commands to `spawn_blocking`

- **Status**: accepted
- **Context**: Four `async` Tauri commands — `scan_directory`, `import_project`, `list_projects`, `rescan_project` — performed blocking I/O directly on the tokio worker thread: recursive `walkdir` tree walks, per-repo `git` subprocess spawns (`git log` / `status` / `diff`), and file reads. A `scan_directory` against a large home directory, or a `list_projects` over many registered repos, can block for seconds. Tokio's multi-threaded runtime runs each `async fn` on one worker until it yields; a blocking call parked on that worker stalls every other IPC command sharing it (the whole UI goes unresponsive for the duration). The runtime's dedicated blocking pool exists precisely for this.
- **Consequences**: The heavy I/O in each command now runs inside `tokio::task::spawn_blocking`, which moves it onto the blocking thread pool and frees the worker to service other commands while it runs. Crucially, the `Mutex<GlobalConfig>` lock is no longer held *across* that I/O — each command snapshots what it needs under a brief lock, does the blocking work lock-free on the pool, then re-locks briefly to apply + `save()`. `list_projects` keys its refresh results by path so the apply pass stays aligned even if the registry changed between snapshot and apply (a project added/removed by a concurrent command simply doesn't match and is left untouched). The closures return their `AppError`s directly (the type is `Send`), so failure paths are unchanged; a join failure (task panic/cancellation) maps to a new `AppError::BlockingTask` variant rather than leaking a raw `tokio::task::JoinError`. Command signatures and return types are unchanged, so the frontend IPC wrappers need no edits. `config.save()` (a brief atomic file write) is intentionally kept on the worker under the lock — only the multi-second walkdir/git work was the problem. The same blocking-on-worker anti-pattern remains in `claude_session.rs` `Drop` and is tracked as its own next step.

## 2026-07-10 — Reap claude child off the tokio worker in `Drop` via `spawn_blocking`

- **Status**: accepted
- **Context**: `ClaudeSession::drop` reaped the child with a synchronous `poll_reap` helper that loops `try_wait()` paced by `thread::sleep` — up to 7s total (5s graceful EOF window, then 2s after `start_kill`). `Drop` can't `.await`, so the sleep was the recommended sync-reap pattern, *but* `ClaudeSession` lives behind `Arc<tokio::sync::Mutex<…>>` and is dropped from inside async Tauri commands — meaning that 7s of `thread::sleep` ran on a tokio worker thread, stalling every other async task sharing that worker (the whole UI, plus any concurrent IPC command on the same runtime). The `Drop`-flavour twin of the blocking-on-worker anti-pattern the `spawn_blocking` command fix had just closed.
- **Consequences**: The reap now runs detached on the blocking pool. `child` became `Option<Child>` so `Drop` can `take()` ownership and hand it to a fire-and-forget `tokio::runtime::Handle::spawn_blocking` closure (along with the stderr-drain `JoinHandle`); dropping the returned `JoinHandle` *detaches* a `spawn_blocking` task (it still runs to completion — distinct from `Child::kill()`, whose future must be awaited to actually send the signal, the checkpoint-4 bug). The graceful-then-forceful sequence is preserved verbatim (close stdin → 5s EOF window → `start_kill` + 2s), so claude still gets a chance to flush its `--resume` session state before SIGKILL; `spawn_blocking` was chosen over `tokio::spawn` + `child.wait().await` specifically to keep that bounded reap rather than immediate-kill-and-detach. Ordering preserved: the stderr-drain task is aborted only *after* the child is reaped, so a verbose child can't fill its stderr pipe buffer and block on exit during the graceful window. A `Handle::try_current()` guard falls back to a synchronous `reap_child` when no runtime is attached (process teardown / drop from a non-runtime thread) — blocking is fine there (nothing async to stall) and it prevents a zombie. `reap_child` was extracted as a free function shared by both paths; `poll_reap` is unchanged. No behavior change observable to callers or the live integration tests (which assert on session semantics, not which thread reaps).

## 2026-07-10 — Resolve `claude`/`git` to absolute, vetted paths at spawn

- **Status**: accepted
- **Context**: Both subprocess spawns handed a bare name to the OS PATH search (`Command::new("claude")` / `Command::new("git")`). With a `.` or empty `$PATH` entry present, the bare-name search resolves against the process cwd — which is a user-selected project for `claude` (where the agent auth token is also injected into the child env) and a scanned repo for `git`. A project shipping a `claude`/`git` script could otherwise run that script under LoopDeck's privileges. This was flagged by the production-readiness audit as "over-broad capabilities + PATH-resolved binaries".
- **Consequences**: A new `binary` module resolves each name against `$PATH` *ourselves* before spawning: it skips every non-absolute `$PATH` component (closing the cwd-hijack vector entirely), vets the candidate is a regular executable file (Unix execute bit), and pins the result in a `OnceLock` so a later `$PATH` mutation can't redirect subsequent spawns. `claude_session.rs` and `git.rs` route through it. Failure semantics split by consumer — `claude` must exist to function (hard `AppError::Agent`), `git` is advisory metadata (soft `None` → callers see "no git info", matching the prior failed-spawn behavior). `git.rs` switched from `current_dir(repo_path)` + bare name to a resolved path + `git -C <repo_path>`. Test spawns (`git.rs` `#[cfg(test)]` setup, `agents.rs` `apply_agent_config` env-inspection tests) are deliberately left bare — they run in temp dirs the test owns or never spawn at all. The module documents two things it *cannot* prevent: a compromised `$PATH` where a legitimate earlier entry wins by design, and the GUI-launch minimal-PATH blind spot (no Homebrew/npm global dirs — discovering common install dirs is a separate follow-up). Closes the audit finding. See loops.md history entry of the same date.

## 2026-07-10 — Top-level React error boundary above `<App>`

- **Status**: accepted
- **Context**: A render-time crash anywhere in the app tree blanked the window with no recovery. The router's `errorComponent` (`router.tsx`) only catches errors thrown *inside* a route — `ThemeProvider`, `RouterProvider`, `Toaster`, and the root `App`'s own render all fall outside its reach and degrade to React's default uncaught-error blank screen. The P2 hardening list called for "a top-level React error boundary above `<App>`". React error boundaries are necessarily class components (`getDerivedStateFromError` + `componentDidCatch`), the only construct that intercepts render/lifecycle/constructor errors.
- **Consequences**: New `RootErrorBoundary` class component (`src/components/shared/RootErrorBoundary.tsx`) wraps `<App>` inside `React.StrictMode` in `main.tsx`. State is `{ error, resetKey }`. Recovery is split into two buttons with deliberately different guarantees: **Reload app** (`window.location.reload()`) is the guaranteed path — LoopDeck is offline-first so on-disk data (`.loopdeck/`, `config.yaml`, the keychain auth token) survives untouched; **Try again** is best-effort — it clears the boundary and remounts the subtree by bumping `resetKey` applied as a `Fragment` `key` (dropping transient React state; Zustand rehydrates from persistence), but a deterministic crash re-trips immediately. The fallback is intentionally self-contained (no Tauri/router/theme/store imports) so it renders even when the provider it wraps is what crashed — it relies only on the base OKLCH tokens in `:root` (`styles.css`), present without `ThemeProvider`. Styling mirrors the nearest sibling, the router's `ErrorComponent` (centered max-w-md card, identical primary/secondary button classes), and `cn()` was dropped for the single unconditional className to match that sibling's plain-string style. A collapsible error-details panel exposes `error.message` + stack for dev diagnostics. Scope is explicitly the render path only: async, event-handler, and `useEffect`-callback errors are not caught by React boundaries and continue to surface through the existing `appStore.error` banner + toasts. See loops.md history entry of the same date.

## 2026-07-19 — Atomic writes via temp-file + fsync + same-dir rename; last-known-good backup for the registry

- **Status**: accepted
- **Context**: Phase 2 of the trust-boundary hardening PRD required critical on-disk state (registry, project config, loops, decisions, PRDs, generated settings, transcripts) to survive crashes and malformed data. The pre-Phase-2 codebase used `std::fs::write` (truncate-then-write) everywhere, and the most dangerous instance was the registry: `GlobalConfig::load` on malformed YAML returned Err, `lib.rs` caught it with `unwrap_or_else(|e| { let fresh = default(); fresh.save(); fresh })` — silently overwriting the malformed primary with an empty default and wiping the user's project list. We considered (1) moving to SQLite with WAL mode, (2) pulling in the `tempfile` or `atomicwrites` crate, (3) hand-rolling a small `persist::atomic_write` helper using the temp + fsync + same-dir rename pattern the filesystem already provides, or (4) just fixing the silent-overwrite in lib.rs and leaving `std::fs::write` elsewhere.
- **Consequences**: Chose (3). SQLite was rejected as a non-goal (the PRD explicitly says "no database, no event-sourcing architecture") and would be a massive schema migration for state that's already YAML/JSON/Markdown. The `atomicwrites`/`tempfile` crates were rejected because the primitive is ~80 lines and full control over the temp+fsync+rename sequence matters (dropping the file handle before the rename for Windows compatibility, PID-keyed temp names so concurrent writers can't collide, cleaning up the temp on error). The helper sits in one new module (`persist.rs`) and is now used by every critical-write site. The registry additionally gets a `.bak` sibling written before every overwrite via `std::fs::copy`, and `load_from_path` runs a 4-step recovery (primary missing → default; primary parses → load; primary malformed → try backup and warn; both malformed → Err). `lib.rs` startup turned the silent-overwrite into a hard `error!` + `exit(1)` — the user gets a structured log line and the malformed file stays on disk for manual recovery, never silently wiped. Test-friendly inner fns (`load_from_path` / `save_to_path`) take explicit paths so the recovery tests don't need an env-var override or a production-code-path detour. Transcript appends stay append-mode (`OpenOptions::append`) with a `flush()` but no `fsync` — per-turn fsync would tank streaming throughput, and the PRD accepts line-atomic appends as sufficient. The `flush()` is enough to make an append visible to a same-process read without waiting for OS writeback. Tradeoff: every critical save now pays an fsync; in practice these are infrequent (registry updates, loop toggles, settings regen — not per-token) so the latency is invisible. See loops.md history entry of the same date.

## 2026-07-19 — ConfirmChanges as the default permission mode

- **Status**: accepted
- **Context**: Phase 1 of the trust-boundary hardening PRD exposed four places where the agent permission story was contradicting itself: (a) the Claude spawn flag ran `--permission-mode acceptEdits` (auto-approving Edit/Write/NotebookEdit inside Claude) while its own doc comment claimed `default`; (b) `skills.rs::setup_hooks` seeded `Edit(*)`, `Write(*)`, and broad build-runner Bash rules into the generated `.claude/settings.json`, short-circuiting LoopDeck's policy at the Claude layer; (c) `PermissionPolicy::allow_by_default()` and `PolicyDefault::Allow` names implied auto-approve-everything while the actual behavior was confirm-first (because `MANUAL_APPROVAL_TOOLS` interception in `answer_control_request` runs before the fallback `decide`); (d) the approval card had no effective-mode indicator. Net effect: all file edits + cargo/npm/go/pnpm/yarn command execution were silently auto-approved, and the UI implied more gating than existed. We considered (1) adding a new `AutonomousProject` mode as the "permissive" option and making `ConfirmChanges` the new strict default, (2) just fixing the spawn flag + allowlist without renaming the types, or (3) making the existing confirm-first behavior honest everywhere — flag, allowlist, type names, and UI — without adding new modes yet.
- **Consequences**: Chose (3). The four-arm `answer_control_request` flow (AskUserQuestion → destructive floor → `MANUAL_APPROVAL_TOOLS` interception → fallback policy) was already confirm-first by construction, so the work was making the other three layers match it rather than building a new mode. Concretely: spawn flag flipped to `default` (routes every un-ruled call through LoopDeck); broad `Edit(*)`/`Write(*)`/build-runner rules removed from the generated allowlist (read-only Bash rules kept); types renamed `PolicyDefault` → `PermissionMode` (`Allow` → `ConfirmChanges`), `allow_by_default` → `confirm_changes`; a `PermissionModeBadge` component surfaces the posture in both agent headers. `AutonomousProject` was *not* added as a type variant despite the initial plan suggesting it as future-proofing — under `ConfirmChanges` both match arms would return `Allow` (the gating happens upstream), so adding it now would be a dead match arm (YAGNI); it lands in Phase 3 alongside the path-containment helpers it actually needs. The `Deny` variant was kept against the plan's suggestion to delete it: the ignored `test_session_deny_path_is_graceful` integration test uses `PolicyDefault::Deny` to verify the CLI recovers from a hard deny without hanging, a real safety property worth keeping verified. Tradeoff accepted per the PRD: file edits the agent used to do silently now park on an approval card, causing more prompts — the "Always allow" button on the approval card is the documented escape hatch for users to add narrow rules (e.g. `Bash(npm run test:*)`) to `.claude/settings.local.json` once they trust a project. See loops.md history entry of the same date.

## 2026-07-19 — Transient gateway-error retry for agent turns

- **Status**: accepted
- **Context**: LoopDeck spawns the `claude` CLI subprocess and consumes its turns as NDJSON. A transient provider failure (Anthropic-compatible `529 overloaded`) does not crash the CLI or surface as a transport error — it arrives as a normal `Ok(AgentResponse { is_error: true, result: "API Error: 529 … overloaded …" })`. Without retry, one transient blip ended the loop run and forced a manual re-prompt. We considered (a) retrying at the HTTP layer inside a hypothetical direct-gateway client, (b) surfacing every `is_error` to the user with no retry, or (c) retrying in LoopDeck based on the error *text*.
- **Consequences**: Chose (c) — a new `retry` module decides eligibility by substring match on the result text (`529` or `overloaded`, case-insensitive), because LoopDeck does not call the gateway directly and there is no structured status code available on this side of the `claude` CLI. Backoff is exponential with a cap (`MAX_ATTEMPTS = 4`, base 2s, factor 2, cap 30s → ~2s/4s/8s then give up). Non-transient errors (401, 400, "not logged in") return immediately — retrying them only delays user feedback. Two wrappers (`send_with_retry`, `send_streaming_with_retry`) sit between the pipeline helpers and `ClaudeSession::send_message[_streaming]`; transcript recording stays *outside* the wrappers so a retried turn is recorded as a single exchange, not N. The streaming wrapper emits a new `ClaudeEvent::Retrying { attempt, max_attempts, backoff_ms, error }` between attempts so the UI can show "Retrying 2/4 in 4s…" instead of a silent double-`Result`; the final `Result` remains authoritative. The substring match is deliberately loose on wording (`overloaded`) and tight on status (`529`) so it survives minor CLI phrasing changes without matching unrelated errors; the test suite pins both positive and negative strings, including the real captured 529 message. Retry against a live overloaded gateway is not unit-testable from this side of the CLI, so the contract is anchored by the substring + backoff tests. See loops.md history entry of the same date.

## 2026-07-10 — Lenient coercion for malformed user hook events; logged exit for unrecoverable Tauri startup

- **Status**: accepted
- **Context**: Two `expect()` sites surfaced in the `panic = "abort"` audit. (1) `skills.rs::find_or_create_matcher_group` called `.expect("hook event must be an array")`; reachable from `setup_hooks` on the import path, a user's pre-existing `.claude/settings.json` with a non-array hook event value (`"Stop": "not-an-array"`) would abort the whole process — the existing guard only created the array when the key was *missing*, not when it was present-but-wrong-typed, so a malformed value flowed straight into the panic. (2) `lib.rs` `.run(tauri::generate_context!()).expect("error while running tauri application")` — the canonical Tauri startup pattern, but under abort a startup failure (WebView/context/plugin init) is a raw panic with no structured record for the user to find.
- **Consequences**: (1) `find_or_create_matcher_group` now coerces a non-array hook event to `[]` and proceeds, mirroring the lenient JSON-parse→`{}` fallback already at the top of `setup_hooks` — malformed user config is treated as "no existing hooks," not a fatal import error (erroring the whole import on one odd hook field would be worse than overwriting it). The surviving `.expect()` is now provably unreachable (coerced immediately above) — an invariant, not a user-input abort. (2) `lib.rs` switched to `if let Err(e) = ...run(...) { tracing::error!(...); std::process::exit(1); }`: same termination outcome, but a structured log line (tracing initialized at the top of `run()`) replaces the raw panic, and the panic/abort source is removed. `std::process::exit` skips destructors, but so does an abort, and nothing meaningful is live at that point. The three remaining production `.unwrap()`/`.expect()` sites (`permission.rs` `split_stages` stages-non-empty ×4, `commands.rs:1018` singleton-vec, `memory.rs:174` re-derived checklist prefix) were classified as provable programmer invariants and left untouched — the audit's concern under abort is panics reachable from bad input, not every unwrap, and converting invariants to defensive `match`es would be churn with no robustness gain. See loops.md history entry of the same date.

## 2026-07-19 — Central registered-project boundary via shared `paths` helpers

- **Status**: accepted
- **Context**: Phase 3 (PRD FR3) required that every project-scoped IPC request resolve through shared registered-root and contained-relative-path helpers, rejecting traversal and symlink escape. Pre-Phase-3, no command verified a path was a *registered* project root — each did `PathBuf::from(&path)` + `if path.exists()` and trusted it. Only `list_dir_entries` and `epic.rs::resolve_spec_path` did any containment checking, and both were local re-implementations. The acute gap was `toggle_prd_loop`: it joined `epic_slug`/`prd_filename` straight into a filesystem path, so a `../` in either escaped `docs/epics/` and read/wrote an arbitrary file; `agent_add_allow_rule` similarly wrote `.claude/settings.local.json` with no checks at all. Correctness depended on each new command independently getting containment right.
- **Consequences**: A new `paths.rs` module is the single chokepoint. `canonical_root(raw)` canonicalizes and requires an existing directory; `resolve_registered_root(&config, raw)` layers a registry check (returns the existing `ProjectNotFound` kind — no new frontend variant — for an unregistered path, the structured "missing/moved" state FR3 calls for); `resolve_within(root, rel, must_exist)` joins a relative path beneath a canonical root and lexically rejects `..`, absolute (`RootDir`/`Prefix`) components before any filesystem access, then canonicalizes the target (read) or its longest existing ancestor (write) and checks `starts_with(root)` — so symlink escape is caught in both directions. `canonicalize_prefix` moved here from `epic.rs` (was a local duplicate) as `pub(crate)`. Every project-scoped command now funnels through these helpers; `import_project` and `remove_project` use `canonical_root` (they are the register/deregister paths and can't require registration). The six pure in-memory slot/session-map commands (`agent_answer_*`, `agent_interrupt`, `agent_pending_*`, `agent_is_busy`) were *deliberately* not routed — they touch no filesystem and their map keys already match the canonical `project.path` the frontend always supplies; routing them would be dead weight and would break `agent_interrupt`'s "Ok(()) whether or not a turn is in flight" contract (FR5 handles removal-during-turn later). Routing `get_project`/`update_description`/`regenerate_description`/`rescan_project` through canonical resolution also fixed a latent bug: they looked the registry up with raw non-canonical input that couldn't match the canonical stored key. `walk_root` (the `@`-mention search walk) now skips symlinks entirely so it can't follow a planted link out of the root; `scan_directory` already used `walkdir::follow_links(false)`. The helpers borrow `config` (not the `AppState` lock) so callers control lock lifetime — required for the `spawn_blocking`-offloaded commands. See loops.md history entry of the same date.

## 2026-07-19 — Bounded untrusted work via central `limits` budgets

- **Status**: accepted
- **Context**: Phase 3 (PRD FR4) required bounding every path that processes untrusted input — recursive scans, file/transcript/event line reads, agent response accumulation, and parked approval/question slots — so a large or adversarial input can't freeze the UI or exhaust memory. Pre-Phase-3b, the discovery scan and `@`-mention walk had a depth cap only (no entry/time/result bound); README/spec/transcript/SKILL reads loaded the whole file into memory; the NDJSON read loops used `tokio::read_line` (unbounded per line — a tool result embedding a huge file could OOM the app); `ResponseAccumulator` grew text/blocks/tool_calls without limit; and a parked approval/question held the per-project turn lock *forever* after the turn-level timeout was removed (it had fired mid-park). The blocking-pool offload, the other half of FR4, was already done. We considered (1) a per-call ad-hoc approach (magic numbers at each site), (2) a config-driven limits file, or (3) one centralized `limits` module of named constants.
- **Consequences**: Chose (3). A new `limits.rs` holds every budget as a `pub const` — scan/search entries+time+results+depth, README/spec/SKILL/transcript byte caps, `STREAM_LINE_MAX_BYTES`, `ACCUMULATOR_MAX_BLOCKS`/`ACCUMULATOR_MAX_BYTES`, and `PARKED_SLOT_TIMEOUT` (10 min) — plus a shared `read_bounded_to_string` (lossy, `AsRef<Path>`). A new `AppError::Limit(String)` (`kind: "limit"`) surfaces breaches that can't degrade to a partial result; the frontend only switches on `kind === "conflict"`, so the error surface is unchanged. Two stance decisions: (a) **walk/scan budgets stop-and-return-partial with a `warn!`** rather than erroring — discovery still surfaces results and the app stays usable, the most defensible reading of "leave the app usable"; the `Limit` error is reserved for single-object loads that can't be partial (an oversized stream line ends the turn). (b) **The accumulator caps memory and stamps a truncation marker, it does not abort the turn** — aborting would orphan the live `claude` process for a bound that's about persisted-response size, not turn correctness; finishing naturally with a marked-truncated response is consistent with the best-effort-channel / always-recorded-transcript architecture. The riskiest change is the new cancel-safe `read_bounded_line` on the hot streaming path: only `fill_buf` is awaited and `consume` is synchronous, so it's safe to race in the `select!` (a dropped partial line is re-read next turn, no loss/duplication); it's pinned by 5 `Cursor`-based unit tests. Parked-slot expiry uses a small `park_or_expire` helper racing the oneshot against the timeout in both arms — on expiry the slot is reclaimed, the request denied so claude unblocks, the approval card resolves to ✗, and the turn ends. The budgets are made injectable for tests (`scan_directory_bounded`, `SearchBudget::with`) since the production constants are far too large to exercise by building real trees. **Deliberately out of scope**, noted as follow-ups: the stderr-drain `read_line` in `spawn` (small diagnostics, not the NDJSON content path), and the small structured-config reads (`project.yaml`, `config.yaml`, `.claude/settings.json`, `.loopdeck/*.md` via `memory.rs`) — neither is the bulk-text input FR4 targets. See loops.md history entry of the same date.

## 2026-07-19 — Split `commands.rs` (2954 lines / ~30K tok) into a `commands/` module tree

- **Status**: accepted
- **Context**: Investigation of a 1.9 MB Claude Code session transcript (the same session that burned ~30M tokens in <1h) showed the dominant token cost was re-reading large backend files across iterations — `commands.rs` alone was read at lines 28 *and* 253 of one session (~50K tokens for one file). At 120 KB / 2954 lines / ~30K tokens, it was the single largest backend file and a near-universal read for any IPC-layer work. The 41 commands and ~20 helpers had natural domain boundaries by naming (`agent_*`, `list_*`, `*_spec_*`) but no module structure — only naming convention held the file together.
- **Consequences**: Pure structural refactor, zero behavior change. `commands.rs` → `commands/{mod, state, composer, project, config_cmds, epics, agent}.rs`. Each domain module owns its private helpers and unit tests; `state.rs` holds the cross-cutting `AppState`, the `DirEntry`/`SkillEntry` wire types, and the lock plumbing (`resolve_root`, `with_session`, `*_slot` accessors, `derive_run_states`, `project_busy`). Largest file drops from **~30K tok → `agent.rs` ~12.6K tok (−58%)**; an agent working on agent-runtime code reads `agent.rs`, one working on project management reads `project.rs` (~5K). All 305 tests still pass (verified identical pre/post split), clippy warning count unchanged (5 → 5, zero new), `cargo fmt` clean. One learning: Tauri's `#[command]` macro emits `__cmd__<name>` items *in the defining module*, so `lib.rs`'s `generate_handler!` must path-qualify (`commands::agent::agent_start_loop`, not `commands::agent_start_loop`) — `pub use` re-exports don't carry the macro-generated items. A `CLAUDE.md`/`AGENTS.md` "Context Discipline" convention (don't re-read files already read this session; prefer `Grep`; cite `file:line`) complements the split by capping per-turn cost. The sibling logic files (`claude_session.rs`, `agents.rs`, `conversation.rs`, `epic.rs`) are already module-sized and were deliberately left alone.

## 2026-07-19 — Transcript-based interruption recovery (no separate run record)

- **Status**: accepted
- **Context**: PRD FR5 / Phase 4 requires that incomplete work — a turn killed mid-flight by an app restart, crash, force-quit, or child/transport failure — be reconciled to a truthful `interrupted` terminal state on startup rather than silently dropped or left looking busy. The pre-existing crash-safety design appends the user turn to `active.jsonl` *before* sending and the assistant turn *after*; a failure between the two leaves a trailing `user` turn with no reply — an orphan. Until now that orphan was silently filtered out by `filter_orphaned_user_turns` on load, so the UI neither showed the interrupted prompt nor explained why it had no answer. FR5 lists "persist a minimal run record" as one option, but the step scoped for the alpha gate added the qualifier: *persist a separate run record only if transcript-based recovery proves insufficient*. The question was whether the orphan in the transcript is itself a sufficient recovery signal (no new persistence layer needed), or whether a separate run-state file/run-ID registry is required.
- **Consequences**: Transcript-based recovery IS sufficient — the orphaned trailing user turn is the signal of incomplete work, so **no separate run record is persisted** (no new file, no run-ID registry, no schema). A new `conversation::reconcile_interrupted(repo_path) -> Result<bool>` reads the active transcript's tail via `limits::read_bounded_to_string`; if the last non-empty line is a `user` turn it appends a persisted synthetic `ConversationTurn::interrupted()` (role `assistant`, `is_error: true`, no `session_id`, body text that says "⏹ Interrupted — …"). The transcript now ends in a terminal assistant turn, so a new turn is unblocked and the UI truthfully renders the prompt + an error-styled "Interrupted" bubble (the frontend already renders `is_error` assistant turns; no TS change). Because the marker carries no `session_id`, `last_session_id` skips it — a follow-up re-spawns with the last *real* id (or no resume), never resuming a non-existent interrupted session. The function is idempotent (no-op on empty / missing / already-terminal / partial-corrupt tails) so it can be called freely. It is wired at exactly two **race-free** sites: (1) **startup** in `lib.rs`, iterating registered projects before the first IPC load (no turn is in flight on a fresh process); (2) the **send-failure path** of all four agent pipelines (`send_and_record`, `send_and_record_streaming`, `start_fresh_and_record`, `start_fresh_and_record_streaming`) via a `mark_turn_interrupted` helper — there the failed send still holds the per-project lock and the turn is definitively over. It is **deliberately NOT wired into the load path** (`load_conversation` / `agent_get_conversation`): a legitimately in-flight turn spends seconds between appending its user turn and its assistant reply, and a concurrent read that reconciled in that window would wrongly stamp a live turn as interrupted. The existing `filter_orphaned_user_turns` stays as defense-in-depth for any orphan still present at load time (e.g. inside read-only archives, or one not yet reconciled), and its doc comment was updated to say so. The "clear stale busy/waiting state" half of the step needed **no code change**: that state is already ephemeral — `claude_sessions` / `pending_answers` / `pending_permissions` / `interrupt_slots` all start as fresh empty maps on restart, and `RunState` is derived *after* `save()` in `list_projects`/`rescan_project` (and `skip_serializing_if = "is_run_state_idle"`) so a non-Idle state is never persisted; the transcript reconciliation is the only durable fix required. Two scope boundaries noted as follow-ups (not alpha blockers): the interrupted marker reuses the `is_error` flag, so the UI badge reads "error" rather than "interrupted" (the body text clarifies; a dedicated variant is a later cosmetic refinement), and `reconcile_interrupted` only touches `active.jsonl` — orphans rotated into archives by a later `agent_start_loop`/`agent_reset_session` are filtered on read, which is acceptable since archives are read-only history that doesn't block a new turn. Parked question/permission expiry (the other Phase 4 sub-item) was already implemented by the bounded-work decision's `park_or_expire` + `PARKED_SLOT_TIMEOUT`. Tests 305 → 313 (+8 covering append/no-op/idempotent/empty/partial-tail/mid-file-orphan/resume-skip); zero new clippy warnings; `cargo fmt` clean.

## 2026-07-19 — `runStartLoop` must always reload turns (frontend matched backend's archive)

- **Status**: accepted
- **Context**: While investigating runaway token usage, the user reported that starting a new loop did not appear to clear the previous conversation — old turns stayed on screen with the new "You are working on this LoopDeck project…" prompt appended on top. Root cause was purely frontend: `AgentPanel.tsx:runStartLoop` only reloaded turns from `active` when switching *from* an archive view (`selectedIdRef.current !== "active"`). When the user was already viewing `"active"` and clicked Start, the reload was skipped — so the UI kept showing the previous loop's turns even though the backend (`spawn_fresh` in `commands/agent.rs`) had correctly archived `active.jsonl` to history and spawned a fresh claude process with `resume_session_id: None` (empty context). The model started clean; the human saw clutter. This UX bug also implicitly encouraged working in one long, growing conversation — which is exactly the behavior driving the context-replay token cost documented in the day's postmortem.
- **Consequences**: One-line semantic fix in `AgentPanel.tsx:runStartLoop`: removed the `if (selectedIdRef.current !== "active")` guard so the reload (`setSelectedId("active")` + `agentGetConversationById(projectPath, "active")` + `setTurns(data)`) runs unconditionally before `runStreamingTurn`. The backend has just archived the old `active.jsonl`, so re-reading `"active"` gives the fresh, empty transcript — matching the backend's actual fresh-start semantics. `selectedIdRef` is still used by `runSendMessage`'s archive-promotion path, so it's not dangling. `npx tsc --noEmit` clean. The visible conversation now clears when starting a new loop, so the LoopDeck workflow no longer encourages unbounded conversation growth. See `docs/postmortem-runaway-token-usage.md` for the broader token-cost story this bug fed into.

## 2026-07-19 — Basic CI on macOS (single-OS alpha gate), plain `cargo clippy`

- **Status**: accepted
- **Context**: Release Gate A's "Basic CI" step required the five core gates — `cargo fmt --check`, Clippy, `cargo test`, `npm ci`, `npm run build` — but explicitly scoped to "the alpha's supported OS rather than a three-OS matrix." The repo had two starter workflows: `build.yml` (tag-triggered installer release across macos/ubuntu/windows — a *distribution* concern, intentionally untouched) and `rust.yml` (a GitHub-template dev CI running `cargo build` + `cargo test` on `ubuntu-22.04` only — no fmt, no clippy, no frontend). The P3 backlog's long-term CI goal is the full macOS/Linux/Windows matrix with `cargo clippy -D warnings` and `tsc --noEmit`, but that is gated on (a) clearing the existing lint debt and (b) signing the cross-OS contract, neither of which is an alpha blocker. Two scoping questions: which single OS, and whether clippy should be `-D warnings` from day one.
- **Consequences**: New `.github/workflows/ci.yml` replaces `rust.yml` (deleted) as the dev CI. It runs a single job on `macos-latest` — chosen as the alpha's primary platform because the developer builds/runs on macOS (darwin) and a private Tauri alpha targets the author's OS first; the formal OS naming lands in the separate "Alpha distribution contract" step. The five gates run sequentially in one job: `cargo fmt --check` → `cargo clippy` → `cargo test` → `npm ci` → `npm run build`. Clippy is plain `cargo clippy`, deliberately NOT `cargo clippy -- -D warnings`: the repo currently carries 6 clippy warnings (lint debt), so `-D warnings` would fail the gate on the very first run; the next release-gate step ("Clear current lint debt") resolves them and then tightens the flag — keeping these as two sequential steps preserves their original intent. `npm run build` already runs `tsc` (type-check) before `vite build`, so a separate `tsc --noEmit` step is redundant and was omitted. Triggers match the old `rust.yml` (push/PR to `main`); `actions/checkout@v5`, `dtolnay/rust-toolchain@stable` (+ `rustfmt,clippy` components), `Swatinem/rust-cache@v2`, and `setup-node@v5` with `cache: npm` mirror the conventions already proven in `build.yml`. No extra system packages are needed on the macOS runner (Tauri's build deps ship via the preinstalled Xcode CLT). Verified locally before commit: `cargo fmt --check` clean, `cargo clippy` exit 0 (6 warnings), `cargo test` 313 passed / 0 failed; the frontend gates run only on the runner (no local `node_modules`). The full three-OS matrix + strict clippy remain the P3 backlog item, deferred until the OS contract is signed.

## 2026-07-19 — Cleared lint debt; CI clippy tightened to `-D warnings`

- **Status**: accepted
- **Context**: Release Gate A's "Basic CI" step shipped with plain `cargo clippy` (not `-D warnings`) precisely because the repo carried 6 pre-existing Clippy warnings — lint debt that would have failed a strict gate on day one. The very next gate step ("Clear current lint debt") was to resolve those 6 warnings and *then* flip the flag, keeping the two intents (clear debt → enforce) as sequential steps. The 6 warnings, all in the Rust backend, were: (1) `dead_code` — `parse_response` in `agents.rs`, a whole-blob NDJSON parser with no production caller; (2) `doc_lazy_continuation` — a ranking list in `composer.rs`; (3) `too_many_arguments` — `ConversationTurn::assistant`, a 9-arg constructor; (4) `needless_borrows_for_generic_args` — `&active_file(...)` passed to an `AsRef<Path>` generic; (5) `redundant_closure` — `.or_else(|| platform_log_dir())` in `logging.rs`; (6) `manual_map` — an `if/else-if-let/else` in `memory.rs::parse_checklist`.
- **Consequences**: Five warnings were straightforward fixes: `parse_response` is genuinely test-only (verified by grep — only callers are in `#[cfg(test)]`), so it is now `#[cfg(test)]` rather than deleted (the canned-NDJSON test fixtures still need it); the `composer.rs` doc got a blank line so "Within a tier…" is its own paragraph; the `&` was dropped on `read_bounded_to_string` (then `cargo fmt` reflowed the `match`); `platform_log_dir` is passed directly to `or_else`; and `parse_checklist` now uses `.map()`. The one judgment call was `too_many_arguments` on `ConversationTurn::assistant`: it takes 9 args because each maps 1:1 to a struct field, and it has ~30 call sites (mostly tests passing positional literals). A params-struct refactor was rejected on the project's own "duplication is cheaper than the abstraction" principle (decision 2026-07-03) — a struct literal wouldn't read better at the test sites, and the three production sites in `commands/agent.rs` still read `response` after the call, so the constructor can't take `AgentResponse` by value without restructuring control flow. So the constructor keeps its signature with `#[allow(clippy::too_many_arguments)]` plus a comment recording the rationale; `too_many_arguments` is the lint Rust specifically marks suppressible for constructors. CI's clippy step is now `cargo clippy -- -D warnings` (with a comment naming the two intentional `#[allow]`s — this one and the `cfg(test)` `parse_response` — so a future reader doesn't mistake them for re-introduced debt). Verified: `cargo fmt --check` clean, `cargo clippy -- -D warnings` exits 0 (0 warnings), `cargo test` 313 passed / 0 failed (same count as before — pure cleanup, no behavior change). The P3 backlog's full macOS/Linux/Windows matrix remains deferred pending the "Alpha distribution contract".

## 2026-07-20 — Defer app notarization / signed artifacts (recurring cost)

- **Status**: accepted
- **Context**: The release plan called for signing/notarizing distribution artifacts — the Parking-Lot item "Sign/notarize artifacts for every OS claimed as supported; do not publish unsigned builds as production releases" and the Current goal's "Public V0.1 adds signed artifacts". Notarizing macOS builds requires an Apple Developer Program membership (~US$99/yr) plus signing infrastructure, a real recurring cost for a solo, local-first alpha that is not yet collecting revenue. The user asked to remove app notarization from the loops for now because it cost money.
- **Consequences**: Removed the "Sign/notarize artifacts" item from the Parking Lot and dropped "signed artifacts" from the Public V0.1 goal. The private alpha (Gate A) and V0.1 (Gate B) now ship unsigned developer builds; the macOS Gatekeeper bypass (`xattr -dr com.apple.quarantine` / right-click → Open) becomes part of the install path captured by the separate, no-cost "Alpha distribution contract" documentation step, which was kept. Notarization is deferred, not deleted: it should be revisited once distribution reach justifies the recurring fee — at which point re-add a release step covering per-OS signing and a notarization gate before publishing production builds.

## 2026-07-20 — Graphify integration is detect-and-surface only (no runtime dep)

- **Status**: accepted
- **Context**: The user asked whether LoopDeck could integrate [Graphify](https://github.com/Graphify-Labs/graphify), an open-source Python tool that turns a codebase into a queryable knowledge graph (`graphify-out/graph.json` + `graph.html` + `GRAPH_REPORT.md`). LoopDeck and Graphify share a thesis — local-first structured memory that helps AI agents understand a project without re-reading everything — but LoopDeck's backend is pure Rust and Graphify is a young YC Python CLI. Taking any runtime dependency (shelling out to the CLI, spawning its MCP server) would expand V1 scope and add a Python install requirement. The question was how deep to integrate.
- **Consequences**: Chose the lightest integration that still delivers value: LoopDeck reads Graphify's JSON output if present and never runs Graphify itself. (1) New `graphify.rs` parses `graphify-out/graph.json` (networkx node-link format: `nodes[]` with `id`/`label`/`community`, `links[]` with `source`/`target`/`confidence` ∈ {EXTRACTED, INFERRED, AMBIGUOUS}) and computes node/edge/community counts, a confidence breakdown, and god nodes by degree centrality; the build date is mined from `GRAPH_REPORT.md`'s first heading. All parsing is infallible — missing/malformed file → `present: false`, mirroring `memory::parse_decisions`. (2) A new `get_graphify_stats` command surfaces the summary through the existing `resolve_registered_root` trust boundary. (3) `DiscoveredRepo` gained a `has_graphify` flag (computed via shared `graphify::is_present`) so the import view can badge projects Graphify has already mapped. (4) The new Graph tab in `ProjectDetail` renders a stats grid, a confidence bar, god-node chips, and Open-graph/report/reveal buttons; an empty-state card points users at Graphify when no graph exists. **Out of scope, as follow-ups:** triggering `graphify` builds (Python-install detection + subprocess lifecycle), managing Graphify's MCP server, and querying the graph from inside LoopDeck (LoopDeck is a PM tool, not a code-exploration tool — querying is the AI assistant's job). E2E-verified against Graphify's own `worked/httpx/graph.json` sample (144 nodes / 330 edges / 6 communities / god-node ranking matches Graphify's report).

## 2026-07-20 — Alpha distribution contract: macOS / Apple Silicon, unsigned, manual `.app` lifecycle

- **Status**: accepted

- **Context**: Release Gate A's "Alpha distribution contract" step required naming **one** supported OS and documenting installation, upgrade/reinstall, rollback, prerequisites, and the diagnostic-log location. Two earlier decisions had already narrowed the field: the 2026-07-19 CI decision chose `macos-latest` as the single-OS dev CI ("the developer builds/runs on macOS … a private Tauri alpha targets the author's OS first; the formal OS naming lands in the separate 'Alpha distribution contract' step"), and the 2026-07-20 notarization-deferral decision dropped signed artifacts for recurring-cost reasons, leaving the macOS install path unsigned. This step had to turn those two priors into an explicit, written contract rather than leave "which OS" and "how do you install it" implied. Open scoping questions were: (a) which single OS to name, (b) whether to narrow `.github/workflows/build.yml` (which still emits Linux + Windows artifacts) to match, (c) whether the alpha gets an auto-updater, and (d) whether to promise cross-alpha on-disk migration.

- **Consequences**: New `docs/alpha-distribution.md` is the authoritative contract. (a) **The one supported OS is macOS on Apple Silicon (arm64)** — the `.dmg` is an arm64 build (`macos-latest`), so it runs natively on M-series Macs; Intel (x86_64) Macs are explicitly *not* supported (no fat/universal binary, Rosetta untested), and no minimum-macOS floor is regression-claimed for the alpha (assume macOS 13+; pin at Gate B). (b) **`build.yml` was deliberately left unchanged** — the release artifact *pipeline* (including smoke-testing install/upgrade) is a separate Gate B item, `build.yml` is tag-triggered and has published no public release yet, and narrowing it now would conflate Gate A documentation with Gate B pipeline work. Instead the contract explicitly scopes the Linux/Windows artifacts `build.yml` *does* emit as "unsupported experiments toward Gate B, not installation-tested, not signed," so the contract and the workflow don't contradict each other. (c) **No auto-updater** — alpha upgrades/reinstalls are manual `.app` replacement; this is honest because the `.app` bundle is stateless (all state lives in `~/Library/Application Support/com.loopdeck.LoopDeck/`, per-repo `.loopdeck/`, and the Keychain), so replacing it never touches user data. (d) **No cross-alpha migration guarantees** — alpha builds may change on-disk shapes without migration code; the contract tells users to keep a build they rely on for rollback. Rollback leans on two existing mechanisms: the atomic-write `.bak` sibling for the registry (2026-07-19 decision) and transcript-based interruption reconciliation (2026-07-19 decision) so an orphaned turn from a rolled-back run self-heals. Prerequisites document the real alpha gotchas surfaced while writing the contract: `claude` must be on `PATH` and is resolved to an absolute vetted path at spawn (2026-07-10 binary decision), with the **GUI-launch minimal-`PATH` blind spot** (Homebrew/npm-global dirs absent when launched from Finder) called out as a known limitation with a terminal-launch workaround; `git` is advisory; the auth token is in the macOS Keychain (service `loopdeck`, account `agent_auth_token`). Diagnostic logs are at `~/Library/Logs/LoopDeck/loopdeck.log.YYYY-MM-DD` (overridable via `LOOPDECK_LOG_DIR`, verbosity via `RUST_LOG`), kept across restarts and not auto-purged.

  **Side-fix discovered while writing the contract:** the `config.rs` doc comments claimed the global registry lived at `~/.config/loopdeck/config.yaml`, but on macOS `directories::ProjectDirs::from("com","loopdeck","LoopDeck").config_dir()` actually resolves to `~/Library/Application Support/com.loopdeck.LoopDeck/` (`~/.config/loopdeck/` is only the Linux/headless fallback). Documenting a wrong path in a *contract* would be worse than having no contract, so the four misleading comments (`load`, `save`, `config_dir`, `config_path`) were corrected to state the real macOS path with the fallback noted — the contract's "where your data lives" table and rollback `cp config.yaml.bak …` snippet now match the code. Comment-only change; `cargo fmt --check` is clean on `config.rs`. The pre-existing untracked `graphify.rs` fmt diff noted here was resolved in a follow-up `cargo fmt` pass on 2026-07-22; the Rust CI gates (`cargo fmt --check` and `cargo clippy -- -D warnings`) are both green. Separately noted: `cargo clippy --all-targets -- -D warnings` surfaces 7 `unused_must_use` errors in `agents.rs` *test* call sites of `ResponseAccumulator::ingest_event` — these are pre-existing test-code lint debt, **not** run by the CI gate (which uses plain `cargo clippy`, no `--all-targets`), so they don't block the alpha; they're cleanup for the P3 strict-Clippy item. The `logging::log_dir()` accessor stays `#[allow(dead_code)]` — surfacing it in a Settings "Reveal logs" button is a noted follow-up, not an alpha deliverable; the path is documented in the contract meanwhile.





## 2026-07-21 — Surface "stuck" AskUserQuestion prompts globally (no ~/.claude reading)

- **Status**: accepted
- **Context**: A user reported a LoopDeck-spawned agent appearing to "freeze and do nothing." Investigation of the Claude session JSONL showed the agent had called `AskUserQuestion` and was correctly parked on a oneshot awaiting the human's answer, but the question picker never rendered in the user's client (they'd locked the Mac / switched windows). LoopDeck already had the pending question in `AppState.pending_answers[path]` (the read loop parks there), and `agent_pending_question(path)` + `agent_answer_question(path, …)` already worked per-project — but the frontend only reconciled a project's slot onto the screen when that project's **Agent tab** mounted. So a prompt that arrived while the user was on any other view was invisible, and the agent sat frozen with no signal. The fix question was whether to (a) read Claude CLI's external `~/.claude/projects/` transcripts to detect stuck sessions from *any* source, or (b) ride on the in-memory slot LoopDeck already owns.
- **Consequences**: Chose (b) — LoopDeck-spawned sessions only, riding the existing `pending_answers` slot + `usePendingInteractions` store. No new persistence, no `~/.claude/` path encoding, no tail-reading of external JSONL, no new answer protocol. Concretely: (1) new `list_pending_questions` command (agent.rs) + pure `collect_pending_questions` helper (unit-tested) snapshots every non-empty slot across the registry; registered in `lib.rs`. (2) `PendingQuestionEntry` type added to the TS mirrors + a no-arg `listPendingQuestions()` IPC wrapper. (3) `usePendingInteractions` gained `reconcileQuestions(entries)` which **replaces** the whole questions map (so resolved prompts drop) and returns the newly-stuck paths (so toasts fire once per prompt, not on every focus). (4) New `useStuckSessions` hook runs the reconcile on app mount + `window` focus + `visibilitychange` (the locked-Mac / switched-window recovery path) — no background polling; uses the exported `router` instance (not `useNavigate`) because `App` sits above `RouterProvider`. (5) Three surfaces: a one-time-per-prompt Sonner `toast.warning` with an "Answer" action that navigates to the project; an amber "Waiting" pill beside the `StatusBadge` on each affected `ProjectCard`; and a tab-agnostic `StuckQuestionCallout` banner at the top of `ProjectDetail` that reuses the extracted `AskUserQuestionCard` (lifted from `Chat.tsx` to its own file) to answer inline via the existing `agent_answer_question` path. `run_state === "waiting"` (already derived in `derive_run_states`) continues to drive the per-card RunButton. **Out of scope:** detecting stuck sessions spawned outside LoopDeck (would require reading `~/.claude/projects/<encoded>/` — a larger separate feature: path encoding, bounded tail reads, a new `sessions.rs` module), background polling, and auto-injecting answers without the user picking.

## 2026-07-22 — Session heartbeat
- **Status**: proposed
- **Context**: AI session active on LoopDeck development.

## 2026-07-22 — Move agent auth token off the macOS Keychain to a local 0600 file

- **Status**: accepted
- **Context**: The agent auth token was stored in the macOS Keychain via the
  `keyring` crate (decision 2026-07-10). Because the app is unsigned /
  un-notarized (decision 2026-07-20), macOS does not trust the code signature,
  so **every** keychain read re-prompts the user for their keychain password.
  The token is re-read from the keychain at every agent spawn
  (`resolve_agent_config` → `secrets::load_auth_token`), so the prompt appeared
  on essentially every message — a severe UX regression for a local-first
  desktop tool.
- **Decision**: Store the token in a dedicated, owner-only local file at
  `<config_dir>/agent_token` (macOS: `~/Library/Application Support/com.loopdeck.LoopDeck/agent_token`),
  written crash-safe via the existing `persist::atomic_write` and locked to
  `0600` on Unix. Kept separate from `config.yaml` so the secret is not mixed
  into the registry that gets inspected / backed up / shared for debugging.
  Removed the `keyring` dependency entirely.
- **Consequences**:
  - No keychain password prompts, ever. The token is read from a plain file.
  - The `secrets.rs` **public API is unchanged** (`load_auth_token` /
    `store_auth_token` / `delete_auth_token`), so no consumer call-site changed
    — `config_cmds.rs`, `state.rs`, `agents.rs`, and the frontend IPC layer are
    untouched at the logic level.
  - The `#[ignore]`d live keychain test is replaced by a hermetic, always-on
    file-backend unit test (plus a 0600-perms check on Unix).
  - The existing Keychain token is **not migrated** (chosen for code
    simplicity): users upgrading past this build must re-enter their token once
    in Settings. Any pre-existing Keychain item is left in place but unread;
    users can remove it manually via Keychain Access. Documented in
    `docs/alpha-distribution.md`.
  - Revisit if/when the app is notarized: a signed build could return to the
    Keychain without the prompt problem. The `secrets` API boundary makes that
    a drop-in change.

## 2026-07-22 — Surface pending permissions cross-project; stop the "Interrupted" bubble from lying
- **Status**: accepted
- **Context**: The recurring `⏹ Interrupted — this turn did not complete. The agent process exited before responding` bubbles (with the user typing `oke / ok / continue` to recover) were misdiagnosed as process crashes. The diagnostic log (`~/Library/Logs/LoopDeck/loopdeck.log.2026-07-22`) showed the real cause: agents parked on manual Bash approvals the user never saw, then hit the 10-min `PARKED_SLOT_TIMEOUT`, auto-denied, and the send pipeline recorded each as the generic "process exited" interruption. Two distinct defects: (1) pending *permissions* had no cross-project surface — `list_pending_questions` + `useStuckSessions` + the dashboard pill + the detail callout existed only for `AskUserQuestion`, so a parked approval in project X was invisible while looking at project Y; (2) the approval-timeout `Err` was conflated with process-death in the transcript/UI.
- **Decision** (two parts):
  1. **Visibility** — mirror the `list_pending_questions` path end-to-end for permissions: `PendingPermissionEntry` + `collect_pending_permissions` + `list_pending_permissions` command (Rust); `PendingPermissionEntry` type + `listPendingPermissions` wrapper (TS); `reconcilePermissions` store action; extend `useStuckSessions` to fetch both kinds in parallel and fire a per-pending-approval toast; a rose "Approve" pill on `ProjectCard`; a tab-agnostic `StuckPermissionCallout` in `ProjectDetail` (reusing the now-exported `PermissionApprovalCard`). No new state — `pending_permissions` already existed and was populated by the read loop.
  2. **Truthfulness** — add typed `AppError::ApprovalTimeout` / `QuestionTimeout` variants (no string-matching); the two `ParkOutcome::Expired` arms return them; a new backwards-compatible `interrupt_kind: Option<String>` field on `ConversationTurn` (`#[serde(default)]`, legacy transcripts load as `None`) plus `interrupted_approval_timeout()` / `interrupted_question_timeout()` constructors; `reconcile_interrupted` refactored into a `append_terminal_if_orphan(path, turn)` helper so the 4 send-failure arms can pick the reason-appropriate marker via a single `mark_turn_terminal(path, &err)` router; `TurnBubble` branches the tag (clock/"timed out" vs AlertTriangle/"interrupted" vs "error").
- **Consequences**:
  - A parked approval is now visible cross-project (toast + pill + callout) within one focus/mount, so the common "Mac locked / switched window" case stops silently auto-denying.
  - The transcript records the *reason* a turn failed, not a blanket "process exited" — the bubble body and tag now match the cause.
  - No behavior change to the 10-min `PARKED_SLOT_TIMEOUT` itself or to auto-deny-on-expiry; a pre-timeout countdown nudge and a dock/window badge were considered and explicitly deferred (toast + per-card pill + per-detail callout mirror the established question flow).
  - `ConversationTurn` gained an optional field; old transcripts deserialize unchanged (`interrupt_kind = None`), covered by a regression test.
  - Pre-existing unrelated failure `epic::tests::test_dogfood_parses_this_repos_epic` (expects 3 PRDs in this repo's 0.2.0 epic, now 4 after an untracked PRD was added) is not addressed here.


## 2026-07-23 — Longer retry budget + friendly exhausted-retry UX for gateway 529s

- **Status**: accepted
- **Context**: A user's LoopDeck agent (intentionally pointed at `api.z.ai`) surfaced a raw `API Error: 529 [...] overloaded` bubble. Investigation showed the retry layer (`retry.rs`) was correctly catching it — the user's exact error string is literally the test fixture — but it gave up after `MAX_ATTEMPTS = 4` over ~14s of sleeps (2s/4s/8s), and the exhausted path returned the raw error response as a normal `Ok(is_error)` result, rendering as a destructive-red bubble with the raw 529 text and no retry affordance. Separately, the `ClaudeEvent::Retrying` event was emitted by the backend (`commands/agent.rs:send_streaming_with_retry`) but **never rendered in the frontend** — so during the retry window the UI looked frozen (the failed attempt's terminal `Result` had landed, the next hadn't started). Two problems: (1) too-short retry budget for sustained z.ai blips, (2) no visibility during retries + a hostile exhausted-retry message.
- **Consequences**: Chose "both" (more retries + better message). (1) **Tuned the retry policy** (`retry.rs`): `MAX_ATTEMPTS` 4→9 (waits now 2s/4s/8s/16s/30s/30s/30s/30s ≈ 2.5 min), and added a hard wall-clock ceiling `BACKOFF_TOTAL_BUDGET_MS = 300_000` (5 min) enforced via a new pure `next_backoff(attempt, elapsed_ms)` helper that combines the count bound (`backoff_ms`) with the budget in one testable place — so a future bump to `MAX_ATTEMPTS` or `BACKOFF_CAP_MS` can't silently extend the worst-case wait past ~5 min. Both `send_with_retry` and `send_streaming_with_retry` switched to `next_backoff` and accumulate `elapsed_ms`. Non-transient errors (401/400/auth) are unaffected — `is_overloaded` is substring-matched on `"529"`/`"overloaded"` only, so a bad token still surfaces immediately. 4 new unit tests cover the new progression + the budget bound. (2) **Surfaced the `Retrying` event**: added the `retrying` variant to the TS `ClaudeEvent` union (it was missing — the backend emitted it but TS never typed it), a `retrying` field on `StreamingState` (set on the event, cleared on the next non-retry `result`), and an inline amber "Gateway overloaded — retrying 2/9 in 4s…" row in `Chat` so the agent doesn't look frozen mid-retry. (3) **Friendly exhausted-retry bubble**: client-side detection (`isOverloadError` mirrors `retry::is_overloaded`) renders an amber (not destructive-red) bubble with an "overloaded" tag and a new `OverloadBanner` — plain-language explanation + a **Retry now** button that re-sends the last user prompt (recovering it from `turns`) via the existing `onSend`, which itself runs the full retry loop again. The transcript still records the raw 529 text (truthful); the banner just renders on top. **Choice: client-side detection over threading a new `error_kind` field through `AgentResponse` + transcript** — keeps the backend change minimal (just retry constants + budget check) and the transcript schema unchanged. **Out of scope:** the non-streaming `send_with_retry` still doesn't emit `Retrying` (only the streaming variant does) — left as-is since streaming is the primary UX; a circuit breaker / failover to a different gateway is a much larger feature (config plumbing for multiple base URLs, health probes) and not in scope. No Retry-button cooldown — a frustrated user can click repeatedly, each spawning a 2.5-min loop; accepted as simple-and-honest.

## 2026-07-23 — Release smoke test is a file-system invariants check, not an app launch

- **Status**: accepted
- **Context**: The first Gate B step — "Define the release artifact pipeline and smoke-test installation plus upgrade/reinstall behavior" — needed an *automated* smoke test that CI and a pre-tag developer run can execute. The obvious-seeming approach is to launch the app and assert it survives an upgrade. But the supported release artifact is an unsigned macOS `.dmg`, and the install/upgrade/rollback contract (`docs/alpha-distribution.md`) makes one promise that is far stronger than "it launches": **state lives outside the `.app` bundle** — replacing the bundle never touches the config dir, the registry, `agent_token`, or per-repo `.loopdeck/`. That promise is the thing a regression could silently break, and it is *purely* a statement about the file system.
- **Decision**: The smoke test (`scripts/smoke-test-release.sh`) does **not** launch the app. It reduces the contract to a file-system invariants check: it stands up an isolated temp "Applications" dir + config dir + repo-with-`.loopdeck/` in `mktemp`, snapshots a path+content `shasum` digest of every user-data path, performs install/upgrade/reinstall/rollback (each = copy/replace the `.app` + strip quarantine), and asserts the digest is byte-for-byte unchanged after each. The rollback step additionally simulates a corrupted registry and proves the documented `cp config.yaml.bak config.yaml` recovery restores the last-known-good copy. Two artifact modes: **hermetic** (default — synthetic `.app` skeletons, portable, no build, the fast CI gate) and **real-artifact** (`--app`/`--dmg` — also asserts bundle structure `Contents/MacOS/LoopDeck` + `Contents/Info.plist`, and for `--dmg` mounts via `hdiutil`). The launch-the-GUI sign-off stays a separate *manual* step (`docs/release-pipeline.md` §6b), because a headless runner cannot drive the webview and the invariants check is what a regression would actually break.
- **Consequences**:
  - The smoke test runs anywhere (Linux CI included for the hermetic mode; macOS-only calls like `xattr`/`hdiutil` degrade to no-ops or gate behind `uname`), with no build dependency in its default mode — so it's cheap enough to run pre-tag and in CI.
  - It catches the real regression class (a future change that writes user data from the bundle/install path) without the flakiness and platform constraints of GUI automation. It does *not* catch runtime regressions (an upgraded binary that crashes on launch) — that's the manual GUI smoke's job, and the split is explicit.
  - The script is fail-stop (first broken invariant → non-zero exit) and hermetic (works only inside its `mktemp` dir with an `EXIT` trap; never touches real `~/Library/Application Support/...`, `/Applications`, or any real `.loopdeck/`), so it is safe to run on a developer machine.
  - `docs/release-pipeline.md` is the single source of truth for the build/release contract (version policy, stages, cut checklist, re-enable-signing runbook); `docs/alpha-distribution.md` remains the install/upgrade/rollback contract; `build.yml` already implements the former and the smoke test asserts the latter's invariants.
  - **Out of scope / deferred:** cross-version data migration is explicitly *not* promised within the alpha series (best-effort forward-compat only); Linux/Windows install contracts (the pipeline compiles+bundles them as EXPERIMENTAL but they are not installation-tested); an auto-updater (upgrades stay manual `.app` replacement); CHANGELOG generation (release notes are hand-authored at cut time).

## 2026-07-23 — LICENSE (MIT) + SECURITY.md threat model; `license` field wired into both manifests

- **Status**: accepted
- **Context**: Release Gate B's unchecked step "Add `LICENSE` and `SECURITY.md` with the agent/subprocess threat model and vulnerability-reporting path" (mirrored by two P5 backlog items). LoopDesk's central security surface — it spawns the `claude` CLI with the operator's full OS privileges — had hardening land across many decisions (binary resolution 2026-07-10, ConfirmChanges 2026-07-19, path containment 2026-07-19, bounded work 2026-07-19, `0600` token file 2026-07-22, unsigned-build defer 2026-07-20), but there was no single document a reporter, reviewer, or new contributor could read to grasp the threat model or to disclose a vulnerability. Two design questions: which license, and how much of the large internal hardening story belongs in a *public* SECURITY.md.
- **Consequences**:
  - **LICENSE** is MIT (copyright "LoopDeck Contributors", 2026). The `license` field was added to **both** manifests (`"license": "MIT"` in `package.json`, `license = "MIT"` in `src-tauri/Cargo.toml`) in the *same* change — a bare LICENSE file with no SPDX field is invisible to npm/crates.io/license checkers and to GitHub's license detection, so shipping one without the other is a half-measure. Verified: `cargo metadata --manifest-path …/Cargo.toml --no-deps` exit 0; `package.json` parses as valid JSON. Holder chosen as the generic "LoopDeck Contributors" (matches `authors = ["LoopDeck"]` and the multi-commit history) rather than a single individual.
  - **SECURITY.md** is written against the *actual code*, not the design intent — every cited symbol was verified to exist (`binary`'s `OnceLock`s for GIT/CLAUDE, `paths::canonical_root`/`resolve_registered_root`/`resolve_within`, `limits::STREAM_LINE_MAX_BYTES`=4 MiB + `PARKED_SLOT_TIMEOUT`=10 min, `agents.rs` `cmd.env("ANTHROPIC_AUTH_TOKEN", …)`, `resolve_agent_config`, and `--permission-mode default` at `claude_session.rs:358`). Structure: a supported-versions table (0.1.x only), private vuln-reporting via **GitHub Security Advisories** (`github.com/suprie/loopdeck/security/advisories/new`) with a 5-business-day acknowledgement SLA (honest about solo-alpha capacity), an explicit in/out-of-scope split, a trust-model table, a **seven-threat model** (T1 subprocess/PATH hijack, T2 destructive agent action, T3 traversal/symlink escape, T4 unbounded-input DoS, T5 auth-token exposure, T6 crash/corrupt state, T7 unsigned-build provenance) — each paired with its mitigation module *and* its residual risk — plus the "prompt injection is inherent (LoopDeck mitigates actions, not reasoning)" caveat and a where-your-data-lives table.
  - **Accuracy fix surfaced while writing it:** the P5 backlog item phrased the model as "subprocess spawn, `acceptEdits`, allow-by-default + destructive floor" — but the spawn flag is `--permission-mode default` (flipped in the 2026-07-19 ConfirmChanges decision; `acceptEdits` is the removed prior, mentioned in the code comment only as what was avoided). SECURITY.md documents `default`; the P5 line was corrected in place when marking it `[x]`.
  - Vuln reporting uses GitHub's private advisory flow rather than a `security@` mailbox: it is encrypted, GitHub-native, CVE-eligible, and needs no separate email infra for a solo project. No invented contact address was added.
  - **Out of scope / deferred** (tracked, not shipped): signed/notarized artifacts (T7), an App Sandbox, a stronger destructive-command analyzer, and bounded log retention (a separate Gate B step).

## 2026-07-23 — Bounded log retention + user-accessible diagnostics

- **Status**: accepted
- **Context**: Release Gate B's unchecked step "Provide user-accessible diagnostics and bounded log retention" (mirrors the P2 item "Cap log retention in `logging.rs` (daily rolling appender grows forever); confirm no `auth_token` is ever logged"). Two coupled gaps. (1) The rolling file appender was `tracing_appender::rolling::daily(&dir, "loopdeck.log")` — daily rotation, **never pruned**, so `~/Library/Logs/LoopDeck/` grew by one file per day forever. `logging::log_dir()` existed but was `#[allow(dead_code)]` ("surfaced for future UI; not yet wired up"), so a user could not find or open their logs from the app. (2) The "confirm no auth_token is logged" clause needed a tangible check, not just an assertion.
- **Consequences**:
  - **Retention is bounded, not manual.** Switched from the legacy `rolling::daily` helper to the `RollingFileAppender::builder()` API with `.max_log_files(MAX_LOG_FILES)` where `MAX_LOG_FILES = 14`. Verified in the `tracing-appender 0.2.5` source (`~/.cargo/registry/.../rolling.rs`) that `max_log_files` prunes **both at construction (startup)** *and* on each rollover (`Inner::new` and `refresh_writer` both call `prune_old_logs`) — so the bound holds even after a long offline gap, not only at midnight. The `filename_prefix("loopdeck.log")` keeps the existing `loopdeck.log.YYYY-MM-DD` naming, so the documented path in `docs/alpha-distribution.md` and any pre-existing log files stay valid. A unit test (`rolling_appender_max_log_files_caps_retention`) seeds 12 dated files, builds an appender with cap 7, and asserts ≤7 survive — proving the approach end-to-end with this crate version. No runtime knob by design (a config-driven cap is one more input to validate and is rarely changed); the constant is the contract.
  - **Diagnostics are user-accessible.** Wired up the dead `log_dir()`: new `LogInfo`/`LogFileInfo` serde types + a pure `collect_log_info_in(dir)` helper (testable against a temp dir) wrapped by `collect_log_info()` (reads the process-global `LOG_DIR`). Two Tauri commands in `commands/config_cmds.rs` — `get_log_info` (folder path, retained files + sizes, total, cap) and `reveal_log_dir` (opens the folder via the OS file manager, same `open`/`xdg-open`/`explorer` argv-only fanout as `open_in_finder`, no shell interpolation). Registered in `lib.rs`. Frontend: `LogInfo` types, `getLogInfo`/`revealLogDir` IPC wrappers, and a new **Settings → Diagnostics** card showing the path, "N files · X KB · keeping the last 14 daily logs", and an "Open logs folder" button (with a stderr-only fallback message when `dir` is null). The reader enumerates file **names/sizes only — never contents** — so the diagnostics surface can't exfiltrate whatever a future log line might contain.
  - **auth_token is never logged — now pinned by a test.** Grep confirmed no tracing macro anywhere interpolates a token value (the three hits — `lib.rs:67`, `config.rs:343`, `secrets.rs:113` — describe the token's *lifecycle*: migration message, save error, file-permission warning; none the value). The only structural path from a token to a log line is `tracing`'s `{:?}`/`?` field formatting, which uses `AgentConfig`'s manual `Debug` impl — already redacted (`***REDACTED***`, covered by `config.rs`'s `test_agent_config_debug_redacts_auth_token`). Added `logging::tests::agent_config_debug_never_renders_token` to pin the invariant from the *logger's* perspective: if someone replaces the manual `Debug` impl with a derived one, this breaks loudly. `docs/alpha-distribution.md` §8 updated: "kept across restarts; not auto-purged" / "not yet wired into the UI" were both stale and corrected.
  - **Verified:** `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean on lib/bin (the green gate); `cargo test --lib` → 344 passed incl. the 4 new logging tests; `tsc --noEmit` clean; `npm run build` clean. **Pre-existing, unrelated, not introduced here** (flagged, not fixed — out of scope): (a) `cargo clippy --all-targets -D warnings` has 7 errors in `agents.rs:1406-1408` test code (`ResponseAccumulator::ingest_event` `#[must_use]`); the green gate checks the lib target. (b) `epic::tests::test_dogfood_parses_this_repos_epic` expects 3 PRDs but this repo now has 4 (Gate C content drift) — confirmed failing on clean HEAD.
  - **Out of scope / deferred:** a runtime-configurable retention cap (the constant suffices for the alpha), log *viewing* inside the app (the "open folder" affordance is enough for V0.1; an in-app tail is a P6 polish item), and fixing the two pre-existing failures above.

## 2026-07-23 — Persist only the selected-project *path* in Zustand; derive the entry (incl. run_state) from Rust

- **Status**: accepted
- **Context**: Release Gate B's unchecked step "Persist only navigation identifiers/preferences in Zustand; reload project and run state from Rust" (mirrors the P6 item "Persist only `selectedProject.path` in Zustand (not the full `ProjectEntry`) to avoid schema drift"). The Zustand `loopdeck-nav` persisted slice stored the **full** `selectedProject: ProjectEntry` object — including its `run_state` (live, derived in Rust per `list_projects`), git metadata, and description. Two defects followed from persisting the object. (1) **Schema drift:** if `ProjectEntry`'s shape changes, a stale persisted object shadows the Rust-backed list until the next reconcile; `useProjects.loadProjects` carried explicit reconciliation code (clear the selection if the path vanished; refresh `last_commit_date`/`last_modified`/`description` if they drifted) purely to paper over this. (2) **Frozen run state:** `run_state` is derived at read time and the entry is `skip_serializing_if = "is_run_state_idle"`, so persisting the object froze whatever run state was live at last close — after a restart the selected project could show a stale "working"/"waiting" instead of the truthful "idle". The question was whether to keep a parallel persisted/reconciled `selectedProject` and just slim it, or stop persisting the object entirely and derive it.

- **Consequences**: Chose to stop persisting the object entirely. (1) **Store** (`src/store/appStore.ts`): `selectedProject: ProjectEntry | null` → `selectedProjectPath: string | null` (an identifier), `setSelectedProject` → `setSelectedProjectPath`. `partialize` now persists only `{ selectedProjectPath, detailTab }` — the path (which project) + the tab (a preference); the full entry is excluded. (2) **Derivation:** a new exported `selectSelectedProject(state)` selector returns `state.projects.find(p => p.path === state.selectedProjectPath) ?? null`. It returns a **stable reference** (an element of the Rust-backed `projects` array, or `null` — never a fresh object literal), so it is safe as a Zustand selector under the default `Object.is` equality (no infinite re-render). `ProjectDetail` reads it via `useAppStore(selectSelectedProject)`. (3) **Dead code removed:** because the entry is always fresh from `projects`, the `loadProjects` reconcile block (removed-project clear + git-info refresh) and its `setSelectedProject`/`updateProjectInStore` bindings are dead — a removed project resolves to `null` and a changed one is automatically current, both straight from Rust. The store mutations (`addProject`/`removeProjectByPath`/`updateProject`/`updateProjectDescription`) now touch only `projects`; `removeProjectByPath` still nulls `selectedProjectPath` when it matches the removed path so the selection doesn't dangle. (4) **Consumers:** `Dashboard.handleSelect`/`handleStart` set the path (not the object; `handleStart` keeps a `projects.some(...)` existence check since it gates agent-start side effects); `ProjectDetail` syncs `selectedProjectPath` from the route param (the route is the source of truth for *which* project is open; the store identifier survives restarts where TanStack Router's memory history — `initialEntries: ["/"]`, in-memory only — does not), and the `OverviewTab` child prop type moved off `ReturnType<typeof useAppStore.getState>["selectedProject"]` to a plain `ProjectEntry | null`. (5) **Schema migration:** bumped the persisted-store `version` 0→1 with a `migrate` that extracts `selectedProjectPath` from the legacy `selectedProject.path`, so an existing user's last-opened project survives the one-time shape change instead of silently resetting. **No Rust change** — `list_projects` already returns project + run state; the frontend simply stopped persisting a parallel copy. Run state is now always live: the selected project's `run_state` reflects the most recent `list_projects`, never a frozen-at-close value. **Verified:** `npx tsc --noEmit` clean; `npm run build` clean (chunk-size warning is pre-existing/unrelated). **Out of scope:** the sibling P6 item "Move module-level mutable `lastSelectedPath` (`AgentRunner.tsx:30`) into the Zustand store" is a separate state-ownership concern (a module-local mutable, not a persisted field) and is not touched here.

## 2026-07-23 — Remove the parked-slot timeout; approvals/questions wait indefinitely

- **Status**: accepted
- **Context**: A user saw an "Interrupted — your approval was needed and the 10-minute limit elapsed. The tool was auto-denied" bubble — the `PARKED_SLOT_TIMEOUT` (10 min, PRD FR4) had fired on a manual approval they never saw, killing the turn. The timeout was written (2026-07-22) as a safety bound *before* the cross-project visibility layer existed: at the time, a parked approval/question was genuinely invisible unless you were on the right Agent tab, so auto-denying after 10 min was the only way to avoid wedging the per-project turn lock forever. Since then the visibility layer landed (toast + ProjectCard pill + detail-view callout, reconciled on app launch + window focus + visibilitychange) and the user explicitly wanted the prompt to "just show the question until it answered" — no expiry. The user chose to remove the limit entirely (Option A: rip out the expiry machinery) rather than keep it as dead scaffolding (Option B), accepting that a genuinely-forgotten approval now holds the project's turn lock until answered or Stopped.
- **Consequences**: Removed the entire expiry path. (1) `claude_session.rs`: deleted `park_or_expire` + the `ParkOutcome` enum (incl. `Expired`); both park sites (question + permission) now `rx.await` directly with only `Resolved`/`Closed` outcomes — no `select!` against a sleep. The only exits are the user answering, or the sender dropping (Stop / session dropped). (2) `error.rs`: removed `ApprovalTimeout` + `QuestionTimeout` variants and their serialize arms. (3) `conversation.rs`: removed `interrupted_approval_timeout()` + `interrupted_question_timeout()` constructors; the `interrupt_kind` field doc now documents `"approval_timeout"`/`"question_timeout"` as **legacy only** (kept so old transcripts still render their truthful "timed out" tag — the frontend `TurnBubble` still handles them). (4) `limits.rs`: removed `PARKED_SLOT_TIMEOUT`. (5) `commands/agent.rs`: `mark_turn_terminal` now emits only the generic `interrupted()` marker for all send failures (the only send failures that still reach it are transport/child/spawn failures, which are genuinely process-exited-shaped). Removed 6 obsolete tests (3 `park_or_expire` unit tests + 3 timeout-kind tests); rewrote the serialization + idempotency tests to use `interrupted()` / `"process_exited"`. **Tradeoff accepted:** a genuinely-forgotten approval now holds the per-project turn lock indefinitely; the only escapes are answering it, hitting Stop (which cancels the turn and drops the sender → the `Closed` path), or the per-project session being dropped. The visibility layer makes "forgotten" much less likely than when the timeout was introduced. **Frontend unchanged:** `interrupt_kind: "approval_timeout" | "question_timeout"` stays in the TS union + `TurnBubble` rendering so historical bubbles keep their tag; no new turn is written with those kinds. Builds clean, `clippy -D warnings` clean, 339 tests pass (the 1 failure is the pre-existing unrelated `epic::tests::test_dogfood_parses_this_repos_epic` PRD-count assertion), `tsc --noEmit` clean.

## 2026-07-23 — `loopdeck:open-pr` skill: stack-agnostic PR shipping with a confirmation gate

- **Status**: accepted
- **Context**: Gate C (Auto-commit + Pull Request) closes the orchestrator's
  build→verify→ship tail so an agent-led feature lands as a reviewable PR instead
  of a pile of staged files. The orchestrator today ends at a `git add` nudge
  (`loopdeck-orchestrator/SKILL.md` Phase 5) — it never commits, pushes, or opens
  a PR. The first Gate C step is the `loopdeck:open-pr` skill. Three design
  tensions had to be resolved up front. (1) **Stack coverage:** LoopDeck imports
  Go, Android/Gradle, PHP, iOS, Ruby, Python, Java/Kotlin, Elixir, Node, and Rust
  projects, so the PR body's Test plan must be *inferred* from the project, never
  hardcoded to one stack — `scanner.rs` detects only 7 marker families, so the
  skill reads markers directly via Bash rather than depending on it. (2) **Tool
  surface:** the skill's only mutating side effects are writing the PR body to a
  tmpfile and appending one line to `loops.md`. (3) **Outward-facing action:** the
  only thing that escapes the local repo is `gh pr create`, which publishes — so
  it must be gated behind an explicit human confirmation, and the explicit
  instruction is `gh pr create --web`.
- **Decision**: Shipped `.agents/skills/loopdeck-open-pr/SKILL.md`, a six-phase
  skill (pre-flight → gather context → generate body → confirm → create →
  record). (1) **`allowed-tools: [Read, Bash, Grep]`** — deliberately no
  `Edit`/`Write`/`Agent`. The body tmpfile (`cat > "$(mktemp)" <<'BODY'`) and the
  `loops.md` insert (portable `awk` + `mv`) are both Bash, making the skill
  explicit about the two artifacts it touches; the skill runs synchronously and
  confirms with the user directly rather than spawning sub-agents. Marker
  detection uses `ls`/`test -f` (no `Glob` tool) so it stays within the declared
  toolset. (2) **Pre-flight:** three read-only checks (`gh auth status`; `git
  rev-parse --abbrev-ref HEAD` rejects `main`/`master`; `git rev-parse
  --abbrev-ref --symbolic-full-name @{u}` rejects a missing upstream), each
  aborting with a one-line remediation. (3) **Body template** (Summary / What
  changed / PRD / Decisions / Test plan) is filled from `git log main..HEAD
  --oneline`, `git diff --stat main...HEAD`, `.loopdeck/decisions.md`
  (most-recent 3), and `loops.md ## Current`; it degrades gracefully
  (commit-derived Summary, `N/A` PRD) when `.loopdeck/` is absent. (4) **Test plan
  inferred from a 13-row marker → command table** (Go/Rust/Node/Android-JVM/
  Maven/PHP/Swift/iOS/Ruby/Python/Elixir + Python-requirements-only + unknown
  fallback); multiple markers emit one block per stack; no marker → the generic
  "Run the project's test suite" line. (5) **Confirmation gate:** Phase 4 prints
  the drafted body and asks proceed/edit/abort; `gh pr create` runs only after
  proceed. (6) `gh pr create --title … --body-file … --web`, then append
  `- [ ] Review & merge: <url>` to `loops.md ## Next Steps`.
- **Consequences**:
  - **`--web` honest about URL capture.** With `--web`, GitHub does not create
    the PR until the human clicks *Create pull request* in the browser, and `gh`
    prints the pre-filled compare URL rather than a final `…/pull/N` URL — so the
    URL recorded in `loops.md` is the creation page (which becomes the PR on
    submit). The skill honors the explicit `--web` instruction (it gives the
    human a final review click and matches the PRD's "confirm, then `gh pr create
    --web`" double-confirmation intent) and documents the headless alternative
    (`gh pr create` without `--web` returns a real `…/pull/N` URL on stdout) for
    when a concrete PR URL is wanted for the memory write.
  - **The Test plan is a reviewer checklist, not a claim of what passed.** The
    skill never *runs* the tests; it emits the stack-appropriate commands as
    unchecked `- [ ]` items plus a Manual line. It cannot lie about test results.
  - **Read-only context gathering; no commit/push.** The skill runs only `gh auth
    status`, `git log`, `git diff`, `git rev-parse` — never `git add`/`commit`/
    `push`/`reset`. Commit and push are owned by the orchestrator's earlier
    phases; `open-pr` opens a PR on work already committed and pushed. The
    "Auto-commit hook point" is the next, separate Gate C step.
  - **Portable `awk` insert** for `loops.md` (inserts under the `## Next Steps`
    heading; works on macOS/BSD and GNU) avoids the `sed -i` portability split;
    falls back to appending a new `## Next Steps` section if absent, and skips
    silently if `.loopdeck/` does not exist.
  - **The skill is independently invocable** (`/loopdeck:open-pr` standalone) and
    will also be wired into the orchestrator's future "Decide & Open PR" phase on
    a green verify verdict — that wiring is a later Gate C step, not shipped here.

## 2026-07-24 — Session heartbeat
- **Status**: proposed
- **Context**: AI session active on LoopDeck development.

## 2026-07-24 — Auto-commit hook point: `open-pr` owns stage → commit → push → publish

- **Status**: accepted

- **Context**: Gate C (Auto-commit + Pull Request) step "Auto-commit hook point"
  — decide where `git add` and `git commit` happen in the orchestrator → ship
  tail. Three facts forced the decision. (1) The orchestrator today ends Phase 5
  with a single `git add` nudge ("add changed file to commit") — it stages but
  never commits, never pushes. (2) The `open-pr` skill shipped 2026-07-23 was
  deliberately read-only about git mutation: its Important Rules said
  "`git commit`/`git push`/`git add` … are out of scope for this skill: commit
  and push are owned by the orchestrator's earlier phases," and its Phase 1c
  pre-flight required the upstream to already be pushed (`@{u}`) — i.e. it opened
  a PR on work someone *else* had committed and pushed. That left a gap: nobody
  owns the commit. (3) The PRD's own lean
  (`prd-verify-and-ship-skills.md` §"Orchestrator wiring", lines 350–353) was
  "keep the `git add` [in the orchestrator], but defer `git commit` to `open-pr`"
  — splitting staging (orchestrator) from commit (`open-pr`). The Gate C step's
  lean refined that: **`open-pr` owns *both* `git add` and `git commit`**, for two
  reasons — the commit message is authored from the verified scope and `open-pr`
  is what produces that scope (the PR body); and `open-pr` is independently
  invocable (`/loopdeck:open-pr` standalone), so a commit living only in the
  orchestrator would strand a standalone invocation that has uncommitted work.

- **Decision**: `open-pr` owns the entire stage → commit → push → publish tail.
  - **Stage + commit** live in a new `open-pr` Phase 5 ("Commit Uncommitted Work
    + Push"), gated by the existing Phase 4 body confirmation. `git add -A`
    stages the feature; if the working tree is dirty, `git commit -m <title> -m
    <Summary>` makes exactly **one** commit whose message is the **verified
    scope** — the same PR title + Summary paragraph the user just confirmed in
    the body — so the commit and the PR describe the feature identically. A clean
    tree skips the commit (the branch tip is already what we want) and just
    pushes.
  - **Push** (`git push -u origin HEAD`) is the only outward-facing action before
    `gh pr create` and runs only after the body confirmation — satisfying the
    step's "no auto-push without the PR body confirmation" constraint. `-u origin
    HEAD` sets the upstream on the first push and is a no-op for the binding
    afterward, so it works whether or not the branch was pushed before.
  - **Pre-flight 1c** changed from "upstream is pushed" (`@{u}`) to "remote
    `origin` exists" (`git remote get-url origin`) — because `open-pr` is now the
    pusher, requiring a pre-existing upstream would be self-contradictory.
  - **Honesty:** Phase 2 captures `git status --porcelain`; Phase 4 discloses any
    uncommitted files and the exact commit message at the gate, so the user sees
    what gets committed before authorizing it. A nothing-to-ship guard (empty
    `main..HEAD` + clean tree) aborts early.
  - **No squash, no history rewrite, no force-push.** Existing WIP commits are
    pushed as-is; the human picks the merge strategy at merge time — resolving
    the PRD Open Question "auto-suggest squashing?" with the PRD's own lean
    (suggest in the body, never execute). A rejected push stops the skill.
  - **Orchestrator:** Phase 5's one-line commit nudge became a hand-off — the
    orchestrator leaves the working tree ready and defers stage+commit+push to
    `open-pr`; it no longer `git add`s. (The full orchestrator renumber — new
    Phase 6 "Verify Against PRD", Phase 6→7, the ASCII diagram, the Phase 2
    plan-template rows, the Memory Convention cross-refs — is the separate
    "Orchestrator wiring" Gate C step and is untouched here.)

- **Supersedes**: (a) The 2026-07-23 `open-pr` decision's "Read-only context
  gathering; no commit/push … owned by the orchestrator's earlier phases" clause
  and its Phase 1c "upstream is pushed" pre-flight — that deferral was always
  marked "the 'Auto-commit hook point' is the next, separate Gate C step," and
  this is that step. (b) The PRD `prd-verify-and-ship-skills.md` §"Orchestrator
  wiring" lean "keep the `git add`, defer `git commit` to `open-pr`" — superseded
  by "`open-pr` owns both." The PRD text is not rewritten (it is a committed
  spec); the wiring step must apply *this* decision (drop the orchestrator's
  `git add`), not the stale PRD lean, when it lands.

- **Consequences**:
  - The commit message is always the verified scope, never an invented or generic
    "WIP" message. One authoring pass (the body) yields both the PR description
    and the commit.
  - `open-pr` is robust standalone: `/loopdeck:open-pr` on a branch with
    uncommitted work commits + pushes + opens the PR in one confirmed action.
  - A single gate (the body confirmation) authorizes commit + push + publish —
    consistent with LoopDeck's confirm-first posture (ConfirmChanges, the body
    gate). No second confirm is added; the body confirmation is the documented
    gate for push.
  - Tradeoff accepted: `git add -A` stages the entire working tree, so a
    well-maintained `.gitignore` is assumed. The Phase 4 disclosure (uncommitted
    files listed) is the honesty backstop — the user sees exactly what is staged
    before authorizing the commit.
  - **Verified:** both `SKILL.md` frontmatters parse; `open-pr` phases renumbered
    1–7 with the new Phase 5; no stale "upstream pushed / out of scope / owned by
    the orchestrator" references remain; the orchestrator's only commit reference
    is the reframed Phase 5 hand-off. No Rust/frontend change (skill text only),
    so `cargo`/`tsc` gates do not apply.

## 2026-07-24 — `loopdeck:prd-verifier` skill: read-only per-criterion PRD verification

- **Status**: accepted

- **Context**: Gate C (Auto-commit + Pull Request) step "`loopdeck:prd-verifier`
  skill." The orchestrator's existing Phase 5 "Final Review" runs stack-specific
  code reviews (`go-code-review`, `ios-code-review`) — those judge code quality,
  not whether the PRD's stated acceptance criteria are met. The PRD
  (`prd-verify-and-ship-skills.md` §"`loopdeck:prd-verifier` skill", lines 121–187)
  calls for a read-only, stack-agnostic skill that parses a PRD's acceptance
  criteria, diffs the changed files, and returns a per-criterion PASS/PARTIAL/FAIL
  table with `file:line` evidence plus a non-goals scope-creep audit, with a
  roll-up verdict the orchestrator's new "Verify Against PRD" phase gates on
  (FAIL → BLOCK, PARTIAL → WARN, all PASS → PASS). Four design points were not
  fully pinned by the PRD and had to be settled in the skill.

- **Decision**: Ship the skill at `.agents/skills/loopdeck-prd-verifier/SKILL.md`
  with `allowed-tools: [Read, Glob, Grep, Bash]` (no Edit/Write/Agent — epic
  ADR-4) and the five-phase parse → diff → check → audit → report flow. The four
  judgment calls:
  - **Diff base is the auto-detected default branch, three-dot merge base.** Not a
    hardcoded `main`: resolve `git symbolic-ref refs/remotes/origin/HEAD` (fall
    back to `main`), then `git diff --name-only <default>...HEAD`. The three-dot
    `...` uses the merge base so only changes *unique to this branch* count. When
    the skill is run *on* the default branch (no branch to diff against), fall
    back to `git status --porcelain` (working tree). This generalizes the PRD's
    `main...HEAD` to `master` repos without adding the optional second argument
    the PRD deferred (Open Question on a diff-base arg — lean: always
    `<default>...HEAD` in 0.3.0; honored).
  - **Stack detection filters build artifacts only, composed per detected stack —
    not a universal union.** The marker → ignore-set table mirrors the `open-pr`
    skill's stack coverage (13 rows + .NET). The `grep -vE` filter is built from
    **only the detected stacks'** ignore dirs (Rust + Node →
    `target|node_modules|dist|build|.next`), so a Node repo's legit
    `vendor/assets/` dir is not dropped merely for sharing Go's `vendor/` name.
    Each name is anchored as a path component `(^|/)...(/|$)` so `src/target.rs`
    survives while `target/debug/…` is filtered.
  - **Evidence is searched in the changed-file set first; unchanged-only evidence
    is flagged.** Every PASS/PARTIAL cites a real `file:line` at HEAD (no evidence
    → FAIL). Integration criteria may legitimately cite unchanged files, but when
    a criterion's only supporting code is unchanged the report flags it (usually
    PARTIAL) — the change under review does not itself satisfy the criterion.
  - **Behavioral criteria that need execution → PARTIAL with a "verify by running"
    note, never a bare PASS.** The skill is read-only and does not build/test/hit
    endpoints; "returns 200" / "handles the error" are judged from code structure.
    If a criterion can only be proven by running it, it is PARTIAL with the
    inferred command (reuses the `open-pr` marker → test-command table), not PASS.

- **Consequences**:
  - The orchestrator gets a single greppable verdict token. The report ends with a
    `Roll-up: BLOCK | WARN | PASS — <N> FAIL, <N> PARTIAL, <N> PASS.` line so the
    new Phase 6 verdict table can key off one token without re-parsing prose.
  - Stack-agnostic by construction: the criteria are quoted verbatim from the PRD
    and never rewritten to an assumed stack; the stack is detected only to filter
    artifacts. A repo with no recognized marker still verifies (common-noise
    filter only).
  - Non-goals audit is a **flag, not a FAIL** — scope creep is surfaced for the
    user to retract or amend the PRD, consistent with the orchestrator's "PRD gap
    → flag to user, don't guess" posture.
  - Tradeoff accepted: the verifier reasons over code structure, not runtime
    behavior, so a structurally-plausible but actually-broken implementation can
    PASS a behavioral criterion only up to PARTIAL. Mitigated by (a) requiring a
    "verify by running" note on such criteria and (b) the orchestrator re-running
    the verify on every rework loop (PRD Open Question lean: re-verify each loop,
    not only at the end).
  - **Verified:** frontmatter parses and matches the `loopdeck:`-prefix +
    one-skill-per-directory convention; the read-only Bash snippets (default-branch
    resolution, `git diff --name-only main...HEAD`, the per-stack `grep -vE`
    filter) were exercised against this real Rust+Node repo — `git diff --name-only
    main...HEAD` returns a real source set with no `target/`/`node_modules/` rows
    (those are gitignored/untracked), confirming the skill's own note that the
    per-stack filter is load-bearing mainly for the on-default-branch working-tree
    fallback. Skill text only (no Rust/frontend change), so `cargo`/`tsc` gates do
    not apply. End-to-end smoke (`/loopdeck:prd-verifier <prd>` producing a
    rendered table) is the separate "End-to-end smoke" Gate C step.


## 2026-07-23 — Per-project Autonomous Mode (unattended loops)

- **Status**: accepted
- **Context**: The multi-loop-overnight workflow ("start loops, go to bed, review the resulting GitHub PRs in the morning") depends on the agent being able to run unattended. Under the default `ConfirmChanges` mode every mutating tool call parks on an approval card, so a loop that hits an approval stalls until the user clicks Allow — and after the 2026-07-23 parked-slot-timeout removal, it now stalls indefinitely (no auto-deny) instead of dying. So a loop that needs an approval overnight neither dies nor progresses; it just parks forever holding the per-project turn lock. The 2026-07-19 decision had explicitly deferred `AutonomousProject` as a future `PermissionMode` variant (a dead match arm under ConfirmChanges); this is that future. Design choices confirmed with the user: per-project flag in `config.yaml` (not global, not `.loopdeck/project.yaml`), destructive floor kept (no `rm -rf` / force-push / `curl|sh` / `sudo` bypass), MCP auto-approves (the project's MCP servers are trusted), and a confirm dialog on enable.
- **Consequences**: The **single behavioral change** is one predicate: `answer_control_request` arm 3 (manual-approval parking) now also checks `!self.policy.is_autonomous()`, so floor-clearing mutating tools fall through to arm 4 (auto-allow) under autonomous mode. The floor in arm 2 (`policy.decide()` → `check_destructive_floor`) is untouched and still runs first — proven by a new `floor_denies_regardless_of_mode` property test iterating all three modes, plus `autonomous_mode_still_enforces_destructive_floor`. (1) `permission.rs`: added `Autonomous` variant (+ `#[default]` on `ConfirmChanges`), `is_autonomous()` getter, the `decide()` arm (Allow, textually identical to ConfirmChanges — the difference is upstream in arm 3), and 4 new tests. (2) `claude_session.rs`: arm-3 predicate gains `&& !self.policy.is_autonomous()`; arm 4 emits a new `decision: "auto-allow"` discriminator (vs `"allow"`) so the UI/transcript can distinguish "you approved" from "the policy approved." (3) `config.rs`: `#[serde(default, skip_serializing_if)] autonomous: bool` on `ProjectEntry` — forward-compat via serde default, no version bump (precedent: `uncommitted`/`run_state`). (4) `commands/project.rs`: `set_project_autonomous` command (mirrors `update_description`: lock → `find_by_path_mut` → save). (5) `commands/state.rs`: new `resolve_permission_policy(state, path)` helper looks up the project's flag under the config lock and returns `Autonomous` or `confirm_changes()`; both spawn sites (`with_session`, the fresh-start path in `agent.rs`) call it instead of hardcoding `confirm_changes()`. Unregistered path → `confirm_changes()` (safest — a stale registry entry can never silently grant autonomy). (6) Frontend: `autonomous?: boolean` on `ProjectEntry`, `setProjectAutonomous` IPC wrapper, `setAutonomous` in `useProjects`, a toggle + confirm dialog in `ProjectDetail` OverviewTab (enable path gated behind the dialog; disable is unconditional), mode-aware `PermissionModeBadge` (amber "Autonomous" vs green "Confirm changes"), an amber "Autonomous" pill on `ProjectCard`, and a per-turn "auto" tag on mutating tool calls in `Chat` (derived from the project flag × `wouldRequireApproval`, not per-event — stable across a turn; precise per-event attribution is a follow-up). **Deliberately NOT done:** the `claude --permission-mode default` CLI flag stays `default` (switching to `bypassPermissions` would defeat the floor at the Claude layer before `answer_control_request` fires); no broad allowlist seeding (`Edit(*)`/`Write(*)`/`Bash(*)` would bypass the floor at the Claude layer too); no new audit-log file (derived from the existing transcript + the `auto-allow` event discriminator); no global toggle (wrong for multi-loop where some projects are trusted and others aren't). **Tradeoff accepted:** a prompt-injected autonomous agent can now auto-edit files within the project and auto-run safe Bash / MCP without asking — the floor still stops the catastrophic cases, but file-level damage is possible. The morning PR review is the intended backstop. **SECURITY.md T2 follow-up:** the threat-model entry for T2 (destructive agent action) should be updated to document the autonomous path + residual risk; flagged for a separate careful pass once the code is settled. **Takes effect on next spawned session** — a live session keeps its current policy until reset (the policy is resolved at spawn time). Verified: fmt clean, `clippy -D warnings` clean, 343 tests pass (incl. the 4 new autonomous ones; the 1 failure is the pre-existing unrelated `epic::tests::test_dogfood_parses_this_repos_epic` PRD-count assertion), `tsc --noEmit` clean.


## 2026-07-24 — One git worktree per independent loop

- **Status**: accepted
- **Context**: The user wants every independent loop of work isolated in its own git worktree — each loop gets its own branch + working directory so its changes are self-contained, reviewable, and can't collide with the main tree or other concurrent loops (notably the multi-loop-overnight workflow from the 2026-07-23 autonomous-mode decision). This applies both to ad-hoc loops (Claude's own iterative work, `/loop` ticks) and to orchestrated PRD→build flows. `EnterWorktree`/`ExitWorktree` are gated behind explicit instruction, so this is recorded as a standing instruction that satisfies that gate. On loop end the worktree is always **kept** (never auto-removed), confirmed by the user, so the work stays on its branch for review/merge.
- **Consequences**: (1) Encoded in the `loopdeck-orchestrator` skill as a **"Worktree Discipline"** section + an **"Important Rules"** bullet, in **both** copies: the active `.claude/skills/loopdeck-orchestrator/SKILL.md` and the shipped `templates/skills/loopdeck-orchestrator/SKILL.md`. The flow is: `EnterWorktree` (named for the feature) before Phase 1 → parallel agents spawned after entering so they inherit the cwd and share the loop's worktree on non-overlapping files → `ExitWorktree({ action: "keep" })` at Phase 6. (2) Mirrored in personal auto-memory (`independent-loops-use-worktree.md`) so non-orchestrated loops honor it too. (3) **Design choice — one worktree per loop, not per spawned agent**: agents already work on non-overlapping files (an existing orchestrator rule), so they safely share the loop's worktree; only same-file contention triggers per-agent `isolation: "worktree"` with a merge-back at the Stitch phase. (4) The leaf dev/review/`api-expert` skills are **deliberately not** given worktree instructions — they run inside a loop's worktree and inherit cwd; they don't own the loop lifecycle. (5) **Pre-existing divergence flagged, not fixed:** the active `.claude/skills/loopdeck-orchestrator/SKILL.md` and the template copy had already diverged on unrelated features (the template carries Epic/PRD back-reference handling in its loops.md memory section that the active copy lacks); only the worktree convention was synced across the two — the rest of the divergence was left untouched as out of scope. Skill-text only (no Rust/frontend change), so `cargo`/`tsc` gates do not apply.


## 2026-07-24 — templates/skills is the SSOT; .claude/skills mirrors it via symlinks

- **Status**: accepted
- **Context**: `.claude/` is gitignored (`.gitignore` line 11), so `.claude/skills/` held local-only copies of skills whose canonical source is the tracked `templates/skills/`. Two copies invite drift — it had already happened with `loopdeck-orchestrator` (the template carried Epic/PRD back-reference content the active copy lacked; flagged in the prior decision's consequence 5). The user declared `templates/` the single source of truth and asked to put the template into the skills.
- **Consequences**: Each `.claude/skills/<name>` is now a **directory symlink** to `../../templates/skills/<name>` — exactly one copy of each skill lives in `templates/`, so drift is impossible and editing a template updates the active skill immediately. **Resolves the prior decision's consequence-5 divergence** — the active orchestrator now carries the template's full content (Epic/PRD back-refs + the worktree discipline). Active set symlinked = the six skills `CLAUDE.md` declares for this repo: `loopdeck-orchestrator`, `loopdeck-vite-senior-engineer`, `loopdeck-rust-expert`, `loopdeck-rust-code-reviewer`, `loopdeck-tauri-expert`, `loopdeck-tauri-code-reviewer` (the four rust/tauri ones were referenced by `CLAUDE.md` but previously absent from `.claude/skills/` — now present). Deliberately **not** symlinked into `.claude/skills/`: the downstream-project skills `loopdeck-go-dev`, `loopdeck-go-code-review`, `loopdeck-ios-dev`, `loopdeck-ios-code-review`, `loopdeck-api-expert` — they serve orchestrated *target* projects, not LoopDeck's own Rust/Tauri/Vite development. Verified: all six symlinks resolve to a `SKILL.md` with the correct `name:` frontmatter, no broken links, orchestrator still discoverable through the symlink. Since `.claude/` is gitignored, the symlinks themselves are local-only (not committed) — the SSOT invariant lives entirely in tracked `templates/`. **Workflow rule going forward: edit `templates/skills/*`, never `.claude/skills/*`** — the latter is now a read-only mirror.


## 2026-07-24 — Orchestrator wiring: new Phase 6 Verify Against PRD, old Phase 6 → Phase 7 Decide & Open PR

- **Status**: accepted
- **Context**: This is the "Orchestrator wiring" Gate C step (the last content step before the end-to-end smoke) of `docs/epics/agent-full-access/prd-verify-and-ship-skills.md`. The `prd-verifier` and `open-pr` skills had landed in `.agents/skills/`, and the 2026-07-24 "Auto-commit hook point" decision had already reframed the orchestrator's Phase 5 commit nudge into an `open-pr` hand-off — but it explicitly deferred the full renumber to this step. The orchestrator's old 6-phase flow ended at "Decide Next Phase," which never verified the implementation against the PRD's acceptance criteria nor opened a PR; this wiring closes that tail so an orchestrated feature ends at a reviewable PR gated by a per-criterion verify verdict.
- **Consequences**: `.agents/skills/loopdeck-orchestrator/SKILL.md` is now a **7-phase** flow. **New Phase 6 "Verify Against PRD"** invokes the read-only `prd-verifier` (citing its `allowed-tools` and the FAIL→BLOCK / PARTIAL→WARN / all-PASS→PASS roll-up) and carries a verdict table that gates the next phase: PASS→proceed, WARN→spawn fix agents + re-verify (no ship until clear or the user accepts the partial), BLOCK→return to Phase 3, non-goals scope creep surfaced as a flag (not a FAIL); re-verify on every rework loop so the verdict is the actual gate. **Old Phase 6 → Phase 7 "Decide & Open PR"**: its outcome table is now keyed on the verify verdict (PASS+integration-green, WARN, BLOCK, integration issues, major gaps, PRD gap, scope creep) and gains a **green-verdict-only `open-pr` branch** — invoke `open-pr` on PASS only; explicitly do NOT call it on WARN/BLOCK (`open-pr` stays independently invocable, but the orchestrator reaches it only through a PASS). Supporting edits: the **ASCII flow diagram** gains a Phase 6 box and a renumbered Phase 7 box (borders kept flush by generating the padded lines with a Python `ljust(54)` so the single-column `→` aligns with the existing `↔`); the **header arrow chain** + intro now read `… → Stitch → Verify → Ship`; the **Phase 2 plan-template** final rows are now Phase 6 "Verify Against PRD" (stitch/test rows kept as lead-in + a `prd-verifier` row) and Phase 7 "Decide & Open PR" (`open-pr`); the **Memory Convention** cross-refs ("Phase 6 Decide Next Phase", "Integration with Phase 6", "(Phase 6 or equivalent)") renumber to Phase 7. Grep confirms no stale `Decide Next Phase` / final-phase `Phase 6` references remain. Skill text only — no Rust/frontend change, so `cargo`/`tsc` gates don't apply. **DIVERGENCE FLAGGED, NOT FIXED (out of scope):** the orchestrator skill exists in two divergent, separately-tracked copies — `.agents/skills/loopdeck-orchestrator/` (canonical per the Gate C PRD; where this whole epic landed) and `templates/skills/loopdeck-orchestrator/` (declared SSOT by the 2026-07-24 "templates is SSOT" decision, and the target of the **live** `.claude/skills/loopdeck-orchestrator` symlink). They have drifted both ways: `.agents/` has the Gate C wiring but not the `templates/`-only "Worktree Discipline" section; `templates/` has Worktree Discipline but not the Gate C wiring (still "6. Decide Next Phase"). Net effect: the orchestrator `/loopdeck:orchestrator` actually invokes (`.claude/` → `templates/`) does not yet run verify→ship. Per the task's explicit "`.agents/skills/` only" scope, and because `templates/` carried pre-existing unrelated WIP, it was not synced here. **Follow-up:** reconcile the two orchestrator copies (and decide whether the `.agents/`-only `open-pr`/`prd-verifier` should also live in `templates/`) and resolve which root is canonical so the verify→ship wiring is effective in the invoked skill. **RESOLVED same day (user chose option 1):** the identical wiring was then applied to `templates/skills/loopdeck-orchestrator/SKILL.md` — header/intro, ASCII diagram (Phase 6 + renumbered Phase 7 boxes), Phase 2 plan-template final rows, the new Phase 6 Verify Against PRD + Phase 7 Decide & Open PR sections, and the four Memory Convention cross-refs — **plus** the Worktree Discipline section's "End of the loop (Phase 6)" → "(Phase 7)" fix the renumber forced (templates is the only copy with that section). The **live** orchestrator (`/loopdeck:orchestrator` → `.claude/skills/` → `templates/`) now runs verify→ship — verified: phase headings 1–7, zero stale `Decide Next Phase` / final-`Phase 6` refs, 8 wiring hits in the resolved file. Remaining **benign, intentionally-unmerged one-way drift** (option 1 was "apply the wiring," not a full merge): `.agents/` still lacks `templates/`'s "Worktree Discipline" + Epic/PRD back-reference sections; functionally inert since `templates/` is the invoked SSOT and is now complete + correct. A full copy-merge (and deciding whether the `.agents/`-only `open-pr`/`prd-verifier` should also live in `templates/`) remains an optional follow-up.


## 2026-07-25 — Milestone 0.2.0 kickoff; hardened-alpha milestone closed

- **Status**: accepted
- **Context**: All three release gates (A/B/C) for the hardened-alpha milestone closed 2026-07-24 — the alpha's narrow release definition is satisfied; the remaining P2/P3/P5/P6 items are non-gate backlog. The next milestone is **0.2.0 — Support Project Management** (`docs/epics/support-project-management/`), already partially built: the spec layer (`epic.rs`) and the epics view + promote-to-loop bridge landed across the `feat/project-management` work (merge `35bdf5c`; commits `e368e98`/`e0795cf`) but were never tracked as the `## Current` loop — exactly the plan/execution drift the spec layer exists to make visible.
- **Consequences**: `## Current` in `loops.md` now tracks the 0.2.0 milestone (started 2026-07-25). Verified completion state: `prd-spec-layer` + `prd-epics-view` are DONE (`epic.rs` parser/structs/milestone-grouping/`done_in_history`/dogfood test; `get_epics` + `get_epics_by_milestone`; `EpicsView.tsx` + `/epics` route; `EpicsPanel.tsx`; `promote_epic_loop` w/ clobber guard; `toggle_prd_loop`); `prd-skill-split` + `prd-epic-author` are NOT STARTED (`loopdeck-orchestrator` is still the single fat skill in both `templates/skills/` and `.agents/skills/`; no runner/author/memory, no managed-skills refresh, no `refresh_skills`). The alpha milestone's gate work is considered complete; its verbose step log was relabeled (not deleted) as the prior-milestone record in `## Current`'s "Last completed" line — the canonical detail stays in these `decisions.md` entries + the Gate C PRD; archiving the verbose log to `loops-archive.md` is a noted follow-up. Its P2/P3/P5/P6 backlog is preserved as milestone-agnostic debt. One open architectural decision is flagged for the skill split: how to reconcile the orchestrator split (a 0.2.0 PRD that predates Gate C) with the 2026-07-24 verify→ship wiring now inside `loopdeck-orchestrator`, and how to reconcile the two divergent orchestrator copies + the `.agents/`-only `open-pr`/`prd-verifier` under the 2026-07-24 `templates/`-is-SSOT rule.

## 2026-07-25 — 0.2.0 skill split + epic-author landed (Option A + full SSOT reconciliation)

- **Status**: accepted
- **Context**: The two remaining 0.2.0 PRDs (`prd-skill-split`, `prd-epic-author`) were coupled and unstarted. `prd-skill-split` (written 2026-07-08) said *remove* `loopdeck-orchestrator`, but the 2026-07-24 Gate-C wiring since added a verify→ship flow (Phase 6 `prd-verifier` → Phase 7 `open-pr`) into it, so a literal split would misplace that wiring. Two scope calls were user-approved before execution: bump the app version to 0.2.0 (so the managed-skills refresh fires), and do **full** SSOT reconciliation (not just the PRD core).
- **Consequences**: Chose **Option A** — keep a slimmed `loopdeck-orchestrator` owning the spawn→stitch→verify→ship flow; add `loopdeck-loop-runner` (single-loop mechanics + a read-context rule that keeps the agent unaware of epics — it follows a loop's `**Epic**`/`**PRD**` back-references as *context*, never a mandate to re-plan), `loopdeck-epic-author` (the drafting aid, authored to its PRD), and `loopdeck-memory` (the `.loopdeck/` write conventions extracted from the orchestrator). The orchestrator's inlined Memory Convention section was replaced with a pointer to `loopdeck-memory`. **Full SSOT reconciliation**: relocated the `.agents/`-only `open-pr`/`prd-verifier` into `templates/skills/` + embedded them in `skills.rs`, and removed the entire `.agents/skills/` duplicate dir (grep confirmed nothing references it — the only non-`.md` hit was a generated `graphify-out` cache; the `.agents` references in `docs/epics/*.md` are historical specs, now stale). `skills.rs` gained a `SkillManifest` + version-aware refresh: `loopdeck-`-prefixed skills overwrite when `APP_VERSION = env!("CARGO_PKG_VERSION")` advances past the manifest's recorded version (numeric `version_gt`, so `0.10.0` > `0.9.0`); same-version existing skills are not clobbered; a missing manifest defaults to `0.0.0` so the first refresh fires; user-owned (non-`loopdeck-`) skills are never touched. App version bumped 0.1.0 → 0.2.0 in `package.json`/`tauri.conf.json`/`Cargo.toml`. New `refresh_skills(project_path)` command (`commands/project.rs`, alongside `rescan_project`) + "Skills" `ActionButton` in `ProjectDetail` OverviewTab re-derive markers via `scanner::quick_scan_directory` and run the version-aware copy on the blocking pool. **Verified**: `cargo fmt --check` clean, `cargo clippy -- -D warnings` clean, `cargo test` 350 passed (+6 new skills tests — version-refresh-overwrite, version-equal-skip, user-owned-untouched, manifest round-trip + missing-default, numeric-semver `version_gt`, core-skills-always-included), `npx tsc --noEmit` clean, `npm run build` clean; all 16 `templates/skills/` frontmatters verified. **Worktree**: implementation on `worktree-feat-0.2.0-skill-split` (kept, uncommitted — per the one-worktree-per-loop rule); the `.loopdeck/` bookkeeping is in the main tree (this entry + `loops.md`). **Follow-ups**: (1) the `docs/epics/agent-full-access/*.md` PRDs still reference `.agents/skills/` as canonical — a doc-sync pass; (2) the `prd-skill-split`/`prd-epic-author` `### Phase` checkboxes were not individually checked because Option A deviates from the literal PRD ("remove orchestrator"); (3) for THIS repo `open-pr`/`prd-verifier` are embedded but not yet symlinked into `.claude/skills/` (the dev repo's symlinks are a manual concern per the 2026-07-24 SSOT decision); (4) manual GUI dogfood — click the new "Skills" button to confirm the install end-to-end.

## 2026-07-25 — Proposed boundary: Markdown for meaning, structured YAML for execution

- **Status**: proposed
- **Context**: The 0.2.0 spec layer exposed duplicate truth across PRD checkboxes, `loops.md` Current/History, epic/PRD frontmatter, and Git. Completion visibility currently joins a planned loop to History by epic + PRD + mutable title. As automatic progress grows, lenient Markdown parsing and title matching are no longer a safe execution-state contract.
- **Consequences**: Added `docs/epics/support-project-management/prd-structured-execution-state.md` for milestone 0.2.1. It keeps epics, PRDs, and decisions in Markdown; introduces stable PRD loop IDs; proposes one schema-versioned `.loopdeck/execution.yaml` holding queue/current/history so completion is one atomic transition; derives progress rather than rewriting PRD checkboxes; separates implemented/committed/in-review/shipped evidence; retains 0.2.x legacy reads and an explicit, reversible `loops.md` migration. This is a proposal only—no runtime format or behavior changed.

## 2026-07-25 — Accept the 0.2.1 structured-execution-state PRD; lock the open-question leans

- **Status**: accepted
- **Context**: The 0.2.1 PRD was drafted `status: proposed` by the `loopdeck-epic-author` session (and registered: README link, `epic.rs` dogfood test bumped 4→5, `decisions.md`/`loops.md` entries). The first 0.2.1 Next Step is to review + accept it. The PRD carries five open questions, each with a recommended "lean" answer. Rather than defer each to its phase, they are locked now so the acceptance is unambiguous and Phase 1 can proceed.
- **Consequences**: PRD frontmatter flipped `status: proposed` → `accepted` (+ `accepted: "2026-07-25"`). The five open questions are resolved to their leans and become the contract for Phases 2–6: (1) **retries reuse the same logical loop ID with a monotonically increasing `attempt`** — progress reflects success if any non-superseded attempt completes; (2) **queue ordering lives in execution state**, not the spec (PRD order = plan, queue order = the user's current scheduling decision); (3) **completed history stays in `execution.yaml` indefinitely for schema v1** — revisit only after a measured repo exceeds a practical size threshold; (4) **LoopDeck does not auto-rewrite legacy `status` from epic/PRD frontmatter** — a separate `lifecycle` field arrives in a later format migration after derived progress is proven; (5) **`loopdeck-state` reuses the Rust domain + persistence code behind a narrow subcommand** — no state transitions implemented in Bash or duplicated in TypeScript. This decision records the stance only; no runtime behavior changed in Step 0. Implementation begins with Phase 1 (stable loop IDs) on branch `feat/0.2.1-stable-loop-ids`.

## 2026-07-25 — Migrate 0.2.0 PRDs to stable IDs; fence-aware `## Phases` parsing

- **Status**: accepted
- **Context**: Phase 1's `update-dogfood-prds` loop was initially scoped to extend the dogfood test only, leaving LoopDeck's own 0.2.0 PRDs ID-less "as the legacy test case." The user asked to put the repo fully on the new format instead. While migrating, the dogfood test surfaced a latent bug: `prd-spec-layer.md` documents the format with a fenced ```markdown block containing a literal `## Phases` heading *before* its real `## Phases` section, and `parse_prd_phases` split the body on `\n## ` without fence awareness — so it matched the fenced `## Phases` first. prd-spec-layer's real checklist items (`Define structs`, …) were never parsed; the app showed `<loop title>` placeholders. The fence-aware `validate_loop_ids` (added this same Phase 1) already skipped fenced examples, so the two were inconsistent.
- **Consequences**: (1) Migrated all 62 real `## Phases` checklist items across the four 0.2.0 PRDs to stable IDs (`spec-layer/…`, `epics-view/…`, `skill-split/…`, `epic-author/…`) via a one-off byte-preserving script with hand-crafted slugs and an exact-count assertion; fenced format-examples were correctly skipped. The repo is now fully on the 0.2.1 format, so the Phase 4 `loops.md`→`execution.yaml` migration will map History entries to those IDs cleanly instead of to `legacy/<id>` unmatched. (2) `parse_prd_phases` is now fence-aware: it blanks fenced lines (`blank_fenced_lines`) before the `## ` heading search, then parses the real section verbatim. Real phase/loop bodies are GFM checklists (never fenced), so blanking fences loses no phase content. Effect: prd-spec-layer's real phases now parse (carrying their migrated IDs); all other PRDs unchanged (their first `## Phases` was already the real one). This aligns the structural parser with `validate_loop_ids` and is locked by `test_parse_prd_phases_ignores_fenced_phases_example`. Dogfood test strengthened from "0.2.0 loops are legacy" to "every loop across all PRDs carries a stable ID." Verified: fmt/clippy(-D)/test(359)/tsc/build green.
## 2026-07-25 — Absolute per-turn deadline backstop for parked prompts

- **Status**: accepted
- **Context**: The 2026-07-23 decision removed the 10-minute `PARKED_SLOT_TIMEOUT` (a footgun — it silently auto-denied manual-approval / `AskUserQuestion` prompts the user never saw) and accepted the tradeoff that a parked prompt holds the per-project turn lock "until answered or Stopped," trusting the cross-project visibility layer (toast / `ProjectCard` pill / detail-view callout, reconciled on launch + focus + visibilitychange) to make a forgotten prompt unlikely. The P2 backlog item this loop closed flagged the residual gap: "unlikely" is not "impossible" — a prompt parked while the laptop is closed overnight, the app is backgrounded, or the user stepped away for hours still wedges the project's turn lock, blocking every later turn on that project (and stalling an unattended autonomous loop).
- **Consequences**: Added `TURN_DEADLINE` = 30 min, an **absolute** (turn-start-relative) backstop — 3× the removed footgun, so a normal meeting/lunch never trips it, yet finite so an abandoned turn frees the lock in bounded time. A single documented constant, not a runtime knob (cf. `logging::MAX_LOG_FILES`). **Scope deliberately narrowed to the parked phase only**: active streaming stays bounded by `READ_LINE_TIMEOUT` (per-read inactivity) + the `ResponseAccumulator` byte/block caps, so this does NOT reintroduce the old turn-level `SEND_MESSAGE_TIMEOUT` that fired mid-active-turn (the footgun whose removal the top-of-file comment used to describe). Mechanism: a pure, unit-tested `park_until(deadline, rx) -> ParkOutcome{Answered,Cancelled,Expired}` helper (separates the deadline *decision* from the park-site *action*, so it is testable without a live session); both park sites (`answer_ask_user_question`, `answer_manual_permission`) race the oneshot against the absolute deadline via `tokio::time::timeout_at`. On `Expired` the site writes the graceful `interrupt` control_request (the live process survives — the next send resumes the conversation, exactly like a manual Stop) and returns `Err`, releasing the lock; the frontend's existing streaming `catch` reconciles (clears the bubble + pending cards + reloads). The deadline is captured once at turn start in `send_message`/`send_message_streaming` and threaded through `answer_control_request`, so multiple parks in one turn share one deadline (total parked time bounded by `TURN_DEADLINE`, not re-reset per park). **Verified**: `cargo fmt --check` clean; `cargo clippy --lib -- -D warnings` clean; `cargo test --lib` 355 passed / 0 failed / 8 ignored (5 new `park_until`/`turn_deadline` tests); `npx tsc --noEmit` clean. **Tradeoff accepted**: a genuinely-abandoned prompt is now ended at 30 min instead of held forever; the parked prompt is abandoned on expiry (the user can't answer it after the fact) — acceptable because 30 min + the visibility layer make "abandoned" the only realistic cause, and the conversation resumes on re-send. **Caveat / follow-up**: the expiry path writes the `interrupt` control_request while claude is blocked awaiting the matching control_response; whether claude processes an interrupt in that state is unverified against the real CLI (the graceful-interrupt arm using the same call only runs during active streaming). If claude does not process it, the next turn degrades to a 180s `READ_LINE_TIMEOUT` then errors (recoverable via reset) — the manual GUI dogfood should confirm claude aborts cleanly. Side-fix: corrected two stale `SEND_MESSAGE_TIMEOUT` doc references (the removed constant) in the park-method + test doc comments. Worktree `worktree-loop-per-turn-deadline`.


## 2026-07-25 — 0.2.1 Phase 2: structured execution state — data layer (`execution.yaml`)

- **Status**: accepted
- **Context**: Phase 1 of `prd-structured-execution-state.md` (stable loop IDs in the spec layer) shipped via PR #8. Phase 2 is the **schema and persistence** layer: the app-managed structured-state counterpart to the human-authored spec (`epic.rs`) and the legacy runtime layer (`memory.rs` / `loops.md`). One YAML file (`.loopdeck/execution.yaml`) holds the current loop, the queue, and the completion history, joined to PRD checklist items by the stable ID from Phase 1. Phase 2 is explicitly the data layer only — the transition commands, the `loopdeck-state` CLI, and the derived-progress UI are Phases 3 and 5. This keeps the change a fully unit-testable Rust module with no IPC surface (and therefore no `tsc`/frontend risk), matching the PRD's Phase 2 task list (types / validation / persistence / recovery / tests).
- **Consequences**: New module `src-tauri/src/execution.rs` (registered in `lib.rs`; `EXECUTION_MAX_BYTES` added to `limits.rs`; `AppError::ExecutionState` variant + serialize arm added to `error.rs`). Types (`execution-types`): `ExecutionState { schema_version, revision, current, queue, history }` + `ActiveLoop` / `QueuedLoop` / `HistoryLoop` / `LoopOrigin` / `Outcome {Completed, Abandoned}` / `GitEvidence`, serde-round-trippable to the PRD's schema. Two deliberate modelling choices beyond the literal schema: (1) **membership in `current` *is* the `in_progress` status** — there is no separate `status` field, so it cannot drift from the derived-state table (the PRD's `status: in_progress` example is an unknown field serde ignores); (2) **`attempt` defaults to 1** and is the retry counter from the resolved open-question lean (same logical ID + monotonic attempt). Validation (`execution-validation`): `validate_invariants()` returns every issue — duplicate IDs in the queue, current↔queue overlap, a current attempt that isn't a new attempt vs the highest completed attempt for that ID (re-run guard that still permits a higher retry attempt), duplicate `(id, attempt)` history records, and an abandoned record missing its reason. `validate()` additionally enforces `schema_version == CURRENT_SCHEMA_VERSION` on write. Three **pure transition helpers** (`complete_current` / `abandon_current` / `promote_next_from_queue`) produce a re-validated next state with `revision + 1`; `complete_current(promote_first_queued=true)` clears current + appends history + promotes in one transition (PRD AC2 — a crash can't leave current cleared but the next loop un-promoted). Persistence (`execution-persistence`) mirrors `config.rs`: `load` does a bounded read and `save` validates → checks the on-disk `revision` against the caller's `expected_revision` (optimistic concurrency — PRD AC3: a stale UI action cannot overwrite a newer state) → best-effort `.bak` → `persist::atomic_write`. Recovery (`execution-recovery`): a malformed primary is **never** overwritten — the last-known-good `.bak` is loaded read-only as `LoadSource::BackupRecovered` with a leading warning (the UI prompts before any restore); a `schema_version` newer than `CURRENT_SCHEMA_VERSION` fails read-only with an upgrade message (PRD: never rewrite a newer schema); older likewise. Tests (`execution-tests`): 23 new tests cover serde round-trip, the OCC stale-writer conflict, every validation rejection (dup queue id, current-in-queue, completed-attempt re-run vs higher retry, duplicate history attempt, abandoned-without-reason), newer/older schema read-only + file-preservation, malformed-primary backup recovery + primary preservation, malformed-primary-without-backup error + preservation, missing-file default, the atomic completion transition (+ promote), abandon's required reason, queue promotion + its guards, and the save guards (refuse invalid / wrong version / write-against-malformed / create-`.bak`). **Verified**: `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean; `cargo test` 387 passed / 0 failed / 8 ignored (23 new `execution::tests`); `npx tsc --noEmit` clean; `npm run build` clean (no frontend change — Phase 2 is Rust-only). **Tradeoff accepted**: the module has no production caller yet, so it carries a module-level `#![allow(dead_code)]` with a "remove when Phase 3 wires `get_execution_state` / `loopdeck-state`" note — the phase-faithful choice over pre-empting Phase 3's command/CLI surface; every public item is exercised by the in-file tests. **Follow-ups**: Phase 3 (state-transition Tauri commands + `loopdeck-state` CLI + skill/hook wiring — the first real callers, at which point the `allow(dead_code)` comes off); Phase 4 (legacy `loops.md` → `execution.yaml` migration); Phase 5 (derived progress UI); Phase 6 (Git delivery evidence). Worktree `worktree-feat-0.2.1-execution-state`.


## 2026-07-25 — 0.2.1 Phase 3 (increment): execution-state transition commands

- **Status**: accepted
- **Context**: Phase 2 landed the `execution.rs` data layer with a `#![allow(dead_code)]` note pending its first production callers. Phase 3's first increment is the **command layer** — the validated IPC write/read paths that make `execution.yaml` reachable and mutable from the UI (and, next, the CLI). This is the `promote-by-id` / `complete-loop` / `abandon-loop` slice of Phase 3. Phase 3 depends on Phase 2 at the code level (it wraps the Phase 2 transitions), so it is stacked in the same worktree rather than branched fresh from `origin/main` (which lacks Phase 2) — the one-worktree-per-loop rule governs *independent* loops, and these are sequential phases of one milestone.
- **Consequences**: New `commands/execution.rs` (registered via `commands/mod.rs` + the five commands added to `lib.rs`'s `generate_handler!`). Commands: `get_execution_state` (read — returns `LoadedExecution` incl. `source` + `warnings`, so the UI can show "recovered from backup"); `promote_loop_by_id` (the `promote-by-id` task — resolves the loop's title + `LoopOrigin {epic,prd,phase}` from the spec layer via a `find_prd_loop` helper keyed on the stable ID, so a PRD-loop title rename after promotion cannot break the execution join; the ID is the only identity the caller supplies); `complete_current_loop` (the `complete-loop` task — optional `commit: Option<String>` → `GitEvidence`, optional `promote_next: bool`); `abandon_current_loop` (`abandon-loop`, required reason); and `promote_next_queued_loop` (wraps `promote_next_from_queue` — added so every public transition has a real caller). New domain transition `ExecutionState::promote_loop_into_current` (sets `current` from a spec loop, computes the retry `attempt = max-completed-attempt + 1`, refuses if `current` is set, re-validates). Every command follows the same shape: `resolve_root` → `execution::load` → transition → `execution::save(&root, &next, loaded.state.revision)` (OCC on the freshly-loaded revision). The `LoadedExecution` / `LoadSource` types gained `Serialize` (command return types). **The module-level `#![allow(dead_code)]` from Phase 2 is removed** — the commands are the first real callers, so every public item is now live (verified by `clippy -D warnings`). Tests: 2 new `commands::execution::tests` (`find_prd_loop` against this repo's real spec layer — the same dogfood approach as `epic::tests::test_dogfood_parses_this_repos_epic` — resolving `structured-state/parse-loop-id` to its origin, plus the unknown-id error path); the transitions + persistence themselves are already covered by Phase 2's 23 `execution::tests`. **Verified**: `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean; `cargo test` 389 passed / 0 failed / 8 ignored (+2); `npx tsc --noEmit` clean (Phase 3 added no frontend — the derived-progress UI is Phase 5). **Remaining Phase 3**: the `loopdeck-state` CLI (`state-command`) so bundled skills/hooks can complete/abandon loops through the validated domain (PRD lean: reuse the Rust domain behind a narrow subcommand; never duplicate transitions in Bash/TS), and `update-memory-skill` + `remove-freeform-runtime-edits` to migrate `loopdeck-memory` / `loopdeck-loop-runner` off free-form `loops.md` edits. **Open CLI design choice**: branch in `main()` on a `state` subcommand (lightest, no new dependency, the PRD permits "the LoopDeck binary"), a separate `[[bin]]` target, or a Tauri sidecar. Worktree `worktree-feat-0.2.1-execution-state`.


## 2026-07-25 — 0.2.1 Phase 3 (complete): `loopdeck-state` CLI + skill migration

- **Status**: accepted
- **Context**: Closes the remaining Phase 3 tasks (`state-command`, `update-memory-skill`, `remove-freeform-runtime-edits`) on top of the Phase 3 command layer. Bundled skills/hooks need a way to complete/abandon loops through the validated Rust domain rather than free-form Markdown edits; the `loopdeck-state` CLI is that bridge, and the memory/loop-runner skills are migrated to route through it where structured state exists.
- **Consequences**: **(1) CLI design — branch in `main()`.** Chose the lightest of the PRD's three permitted surfaces: `main.rs` checks whether the first arg is `state`; if so it calls `loopdeck_lib::run_state_cli(rest)` and exits with its code, otherwise it runs the desktop app exactly as before. No `clap`, no new dependency, no separate `[[bin]]`/sidecar packaging surface. A normal GUI launch never passes `state` first, so it is unaffected (Finder `-psn_…` args and `tauri dev` pass-through land in the `else` arm). **(2) `state_cli.rs`** — a pure `parse_state_cli(&[String]) -> Result<StateCli, String>` (flags accept both `--name value` and `--name=value`; `--path` defaults to cwd) feeds an `execute(&StateCli)` that reuses `execution::load` / the `ExecutionState` transitions / `execution::save` and `epic::find_loop_by_id` — no transition rule is duplicated (the PRD's hard lean). Surface: `show`, `promote <loop-id>`, `complete [--commit <sha>] [--no-promote-next]`, `abandon --reason <text> [--no-promote-next]`. Exit codes: 0 ok, 1 domain/IO error (stderr), 2 bad usage (stderr + usage block). **(3) Shared spec lookup** — extracted `epic::find_loop_by_id` + spec-native `epic::LoopLocation` so the IPC command and the CLI share one lookup (the IPC `find_prd_loop` is now a thin wrapper); keeps the layer direction one-way (runtime knows about spec, not vice-versa). **(4) Skill migration** — `loopdeck-memory` gained a "Structured execution state (`execution.yaml`)" section placed before the legacy `loops.md` format: detect mode via `test -f .loopdeck/execution.yaml`; in structured mode transition loops ONLY via the `loopdeck state` table (never hand-edit `execution.yaml` — that bypasses validation + the OCC guard), `decisions.md` is still hand-edited, and `loops.md` is a legacy artifact; the legacy `loops.md` format is retained for unmigrated projects (Phase 4 migrates). `loopdeck-loop-runner`'s completion step is now mode-aware (structured → `loopdeck state complete/abandon`; legacy → move to History), keeping the origin-PRD-box check + `decisions.md` append in both modes. This is `update-memory-skill` + `remove-freeform-runtime-edits`: structured-mode loops are never transitioned by free-form Markdown edits. **Verified**: `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean (lib + bin); `cargo test` 398 passed / 0 failed / 8 ignored (+9 `state_cli::tests` — pure parser cases + an end-to-end `run_state_cli` happy path); `npx tsc --noEmit` clean (Phase 3 added no frontend). **Binary smoke-tested**: `loopdeck state show --path /tmp` → revision-0 default summary, exit 0; `state frobnicate` → exit 2 + usage; `state complete` with no current loop → exit 1 + the Conflict message — confirming `main.rs` arg-routing + the full CLI path work at runtime, not just in-unit. **Phase 3 is complete.** **Follow-ups**: Phase 4 (legacy `loops.md` → `execution.yaml` migration — the bridge that lets existing projects adopt the structured path the skills now prefer); Phase 5 (derived progress UI consuming `get_execution_state`); Phase 6 (Git delivery evidence). Skill text + Rust only — no `tsc`/frontend gate beyond confirming it is unaffected. Worktree `worktree-feat-0.2.1-execution-state`.


## 2026-07-25 — 0.2.1 Phase 4: migration & compatibility (`loops.md` → `execution.yaml`)

- **Status**: accepted
- **Context**: Phases 1–3 made `.loopdeck/execution.yaml` the canonical store and routed bundled skills through the validated `loopdeck state` CLI — but only for projects that *already* have it. Phase 4 is the bridge that lets an existing project still on legacy `.loopdeck/loops.md` adopt the structured path, while (a) losing no data, (b) never guessing identity, and (c) keeping the legacy read path working through 0.2.x. The PRD's migration flow (`prd-structured-execution-state.md` §"Migration and Compatibility") and acceptance criteria 5–10 framed it; two genuine forks needed resolving up front.
- **Consequences**:
  - **New `src-tauri/src/migration.rs`** — a pure `build_preview(legacy, spec, now)` + crash-safe idempotent `apply(repo, now)`, plus `preview(repo, now)`. `parse_loops_for_migration` captures the `**Epic**`/`**PRD**` back-references the lenient `memory::Loop` drops (reuses `memory::parse_kv_line`/`split_heading`, now `pub(crate)`, + a new `memory::loops_md_path`); the spec index is built from `epic::parse_epics`.
  - **Fork 1 — rename, not copy (user-confirmed).** Confirmed migration `fs::rename`s `loops.md` → `loops.legacy.md` (after writing `execution.yaml`). The PRD flow said "copy" but its Detection rules describe a `loops.md`+`execution.yaml` coexistence that would leave a stale, writable duplicate — the exact duplicate-authority bug the PRD kills. Rename leaves the project cleanly structured with the original preserved verbatim. Crash-safe order: write `execution.yaml` first (validated + atomic), then rename (POSIX-atomic); every crash point is recoverable and `apply` is idempotent (an existing `execution.yaml` + lingering `loops.md` + no `loops.legacy.md` just completes the rename).
  - **Fork 2 — exact-only matching, no fuzzy.** A record is matched by exact `(epic, prd, title)` when it carries the bridge back-ref, else by exact title-only equality across every PRD loop; **exactly one** match assigns the ID, zero/many → unmatched. Title-only exact equality is not fuzzy, so it recovers correct mappings for hand-authored records without guessing (criterion 6). A `seen`-set dedups IDs across current/queue/history so the planned state is always valid.
  - **Unmatched records are never written to `execution.yaml`** — they're preserved verbatim in `loops.legacy.md` (no data loss) and surfaced in the preview report (`UnmatchedRecord { label, title, section, reason }`). **No schema change** to `ExecutionState` (no `migration_status` field — that bloats the canonical store); the report carries the unmatched list for the reconciliation panel, Phase 5's derived UI reads `execution.yaml` only.
  - **IPC** (`commands/execution.rs`): `get_migration_preview` (read-only) + `apply_migration` (confirmed write), both via `resolve_root`; `LoadedExecution.file_present` (derived from `LoadSource`) is the mode signal so the UI branches without a probe. **CLI**: `loopdeck state migrate [--yes]` (`state_cli.rs`) — preview is read-only, `--yes` applies, reuses the domain (no duplicated transitions). **Frontend**: execution-state TS types + wrappers + a project-level `MigrationCard` (preview summary, expandable unmatched reconciliation list, confirm-gated Migrate) in a mode-branching `LoopsPanel` (structured → render `execution.yaml` current/queue/history; legacy → legacy view + card). The structured render is the minimal Phase-4 shape; full derived PRD/epic progress is Phase 5.
  - **Skill accuracy fix**: `templates/skills/loopdeck-memory` was already mode-aware (Phase 3 routes structured projects through `loopdeck state`, so it won't recreate the renamed `loops.md`); its "Phase 4 will offer migration" note was updated to point at the `loopdeck state migrate` CLI + the UI card. The global `~/.claude/hooks/loopdeck-memory-write.sh` (external) still appends to `loops.md` and could recreate a stale copy post-migration — flagged as a follow-up, out of Phase 4 scope.
  - **Verified**: `cargo fmt --check` clean; `cargo clippy -- -D warnings` clean (lib+bin); `cargo test --lib` 415 passed / 0 failed / 8 ignored (+17: 11 pure matching/preview + 5 `apply` I/O + 1 CLI parse); `npx tsc --noEmit` clean; `npm run build` clean. **Binary-smoked**: `loopdeck state migrate` preview is read-only (no `execution.yaml` written — criterion 7), `--yes` writes `execution.yaml` + renames `loops.md`→`loops.legacy.md` verbatim (criterion 8) with unmatched records surfaced not written (criterion 6), and re-runs are idempotent no-ops. A dogfood test migrates this repo's own `loops.md`+spec to a valid `ExecutionState`. **Follow-ups**: Phase 5 (derived progress UI — planned/queued/in-progress/implemented/shipped with the spec join + discrepancy surfacing, consuming `get_execution_state`); Phase 6 (Git delivery evidence). Worktree `worktree-feat-0.2.1-migration`.

## 2026-07-26 — Agent execution-plan approval (`ExitPlanMode` interception + plan-mode toggle)

- **Status**: accepted
- **Context**: Ad-hoc request (branch `claude/agent-execution-plan-approval-396750`, unrelated to the 0.2.x milestone above): when Claude runs under the CLI's `plan` permission mode and finishes planning, it calls the `ExitPlanMode` tool to ask permission to start executing. `claude_session.rs` had no handling for this at all — the tool wasn't in `MANUAL_APPROVAL_TOOLS`, so an `ExitPlanMode` call would have silently fallen through to arm 4's auto-allow (no human ever saw the plan), and nothing in LoopDeck ever put the CLI into `plan` mode in the first place (spawn hardcodes `--permission-mode default`), so the path was previously unreachable. Confirmed the wire protocol against the installed `claude` CLI binary (`strings` on the bundled JS): `ExitPlanMode`'s tool input is `{"plan": "<markdown>"}`, and a live session's permission mode can be changed mid-conversation by writing a `{"type":"control_request","request":{"subtype":"set_permission_mode","mode":"plan"}}` line to stdin (the CLI accepts `set_permission_mode` as one of a small allow-listed set of client-initiated control requests, alongside the existing `interrupt`).
- **Consequences**: **(1) Interception** — `ExitPlanMode` is now Arm 0 in `answer_control_request` (checked before `AskUserQuestion`), parking on a new dedicated `PlanSlot` (not `PermissionSlot`, so a plan approval can never collide with an unrelated tool approval) via `answer_plan_approval`, which mirrors `answer_manual_permission`'s park-then-write shape but carries plain `Decision` (no `updatedInput`) and adds an autonomous-mode short-circuit (an autonomous project auto-allows the plan — unattended operation shouldn't gain a new manual gate). Surfaced as `ClaudeEvent::PlanApproval { request_id, plan, decision, reason }`, same `pending`/`allow`/`deny`/`auto-allow` vocabulary as `PermissionRequest`. **(2) Reachability** — added `ClaudeSession::ensure_permission_mode(plan_mode: bool)`, called at the top of `send_message_streaming` before the user-turn line is written (fire-and-forget `set_permission_mode` control_request over the same stdin, relying on the CLI processing lines strictly in order); a `plan_mode_active` field tracks the last mode asked for so a turn that leaves plan mode without ever getting an approved `ExitPlanMode` (Stop, error, or the model just answering directly) is defensively reset to `default` on the next non-plan send — otherwise a stuck flag would silently keep every later turn read-only-restricted. Threaded as a new `plan_mode: bool` argument on `agent_send_message_streaming` and a `planMode` composer toggle (`ListChecks` icon button) in `Chat.tsx`, owned by `AgentPanel` as one-shot UI state (reset to `false` the instant a send fires, mirroring Claude Code's own shift-tab revert). **(3) Argument-count refactor** — bundling `question_slot`/`permission_slot`/`plan_slot` into a new `ParkSlots<'a>` struct (distinct from `InterruptSlot`, which serves the unrelated Stop-button purpose) was necessary to keep `answer_control_request` / `send_message[_streaming]` / `send_streaming_with_retry` under clippy's `too_many_arguments` threshold once a fourth slot was added — a mechanical but repo-wide signature change across `claude_session.rs` and `commands/agent.rs`. **(4) Full IPC/UI parity** with the existing question/permission surface: `agent_answer_plan` / `agent_pending_plan` / `list_pending_plans` commands, `AppState.pending_plans`, a `PlanApprovalCard` (renders the plan as markdown via the existing `Markdown` component, Approve/Reject with an optional reject-feedback textarea), `pendingInteractions` store (`plans` map + `reconcilePlans`), and `useStuckSessions` cross-project toast reconciliation — so a plan parked while the user is on another view/tab is surfaced exactly like a stuck approval or question, not silently missed. `derive_run_states` (`RunState::Waiting`) also keys off `pending_plans` now. **Verified**: `cargo fmt` clean; `cargo clippy --lib -- -D warnings` clean; `cargo test --lib` 418 passed / 0 failed / 8 ignored (+6 new: 3 `parse_exit_plan` unit tests, no new integration tests since exercising a real `plan`-mode round-trip needs a live `claude` session); `npx tsc --noEmit` clean; `npm run build` clean. **Not verified**: an actual end-to-end `plan` → `ExitPlanMode` → approve/reject round-trip against a live Claude session (no API credentials available in this environment) — the `set_permission_mode` wire shape was confirmed by inspecting the installed CLI binary's bundled source, not by observing it on the wire. **Follow-up**: manual GUI dogfood (toggle Plan mode, send a message, confirm the plan card renders and Approve/Reject both resume the turn correctly) is the remaining human step before this is fully trusted.

## 2026-07-26 — Provider-neutral harness sessions with Codex app-server

- **Status**: accepted
- **Context**: LoopDeck's agent pipeline was hard-wired to persistent Claude stream-JSON sessions. Codex needs equivalent streaming, resume, approvals, user questions, and interruption without duplicating the command and UI pipelines or sharing provider credentials.
- **Consequences**: Commands now hold a provider-neutral `HarnessSession` selected by `AgentConfig.harness`. Claude remains the default for backward-compatible config files. Codex runs a persistent `codex app-server` JSONL session, maps its events and server requests into LoopDeck's existing conversation/control surfaces, and relies on the installed Codex CLI account and provider configuration rather than Claude's token or base URL. Codex transcript IDs are stored with a `codex:` prefix, and untagged legacy IDs remain Claude-owned, preventing accidental cross-provider resume. Switching harnesses replaces an idle cached session; an active session finishes safely and is replaced on the next send.


## 2026-07-26 — Route Codex actions through LoopDeck's approval boundary

- **Status**: accepted
- **Context**: Codex's `workspaceWrite` + `on-request` preset permits commands and file edits inside the workspace without an app-server approval request. That bypasses LoopDeck's default confirm-changes UI and prevents its destructive-command floor from evaluating actions that Codex performs autonomously.
- **Consequences**: Codex turns always use `readOnly` + `on-request`, forcing commands and edits through app-server approval requests. LoopDeck's existing `PermissionPolicy` then parks normal projects for a user decision, auto-allows safe requests in autonomous projects, and denies destructive commands before autonomy is considered. Codex effort is cleared when switching providers and defaults to the model's advertised setting unless the user explicitly supplies an override. Regression tests pin turn security parameters, confirm/autonomous/floor decisions, approval context mapping, and idle/busy harness replacement.

## 2026-07-26 — Phase 4 post-merge migration hardening

- **Status**: accepted
- **Context**: Reviewing merged PR #11 found three edge cases in the Phase 4 migration surface: POSIX `rename` may replace an existing `loops.legacy.md`; the UI offered migration only when legacy current/history records existed, excluding empty and next-step-only files; and queued structured records were labeled completed.
- **Consequences**: Fresh migration now refuses before writing `execution.yaml` when `loops.legacy.md` already exists, preserving the earlier snapshot byte-for-byte; a regression test pins the no-write behavior. The Loops panel now offers migration whenever `loops.md` exists, reconciliation details include unconverted next steps, and queued records render as queued. Phase 4's PRD checklist is closed after post-merge verification.

## 2026-07-26 — Install and discover skills per agent harness

- **Status**: accepted
- **Context**: LoopDeck installed and listed every project skill exclusively from `.claude/skills`. Codex repository discovery uses `.agents/skills`, so Codex sessions could neither fetch the bundled LoopDeck skills nor surface Codex-only project skills in the composer.
- **Consequences**: Managed skill installation now targets both `.claude/skills` and `.agents/skills`, with an independent `.loopdeck-manifest.json` in each root so Refresh Skills backfills a missing Codex installation even when Claude is already current. Composer discovery snapshots the configured harness and reads only its native root. This duplicates the small bundled `SKILL.md` files intentionally; symlinks were rejected because LoopDeck is cross-platform and each root needs independent refresh state.

## 2026-07-27 — Bound graceful Codex interruption and replace wedged children

- **Status**: accepted
- **Context**: Removing the false active-turn stdout timeout correctly allowed long silent tools, but exposed a distinct failure mode: a genuinely wedged app-server could ignore `turn/interrupt`. LoopDeck then waited indefinitely for `turn/completed`, leaving the per-project mutex and UI stuck on "Agent is working" even after Stop was pressed.
- **Consequences**: Once Stop is requested, Codex gets a 15-second grace period to complete the interrupted turn. If the deadline expires, LoopDeck closes stdin, terminates the child, returns a specific harness error, and clears the normal turn/interaction state through the existing error path. The dead session reports itself unusable; `with_session` replaces an idle unusable Codex cache entry on the next send while still preserving any currently locked in-flight session. Active turns remain unbounded before Stop, so legitimate long-running silent tools are not reclassified as stuck.

## 2026-07-27 — Provider boundary returns evidence, not derived status

- **Status**: accepted
- **Context**: Phase 6 initially defined `DeliveryProvider` as returning `DeliveryStatus`. That made `InReview` and `Shipped` circular and unconstructable in core production code—the provider could only return a derived status it had already created—and strict dead-code validation correctly rejected both variants and the unused trait.
- **Consequences**: Providers now return neutral `DeliveryEvidence { in_review, shipped }`. The progress read model maps that evidence into `InReview` or `Shipped` only after a reachable local commit establishes `Committed`; missing providers and provider errors retain the strongest local state. This activates the optional provider boundary without suppressing dead-code warnings or making remote access an offline runtime dependency.
## 2026-07-27 — Derived progress lives in EpicsPanel/EpicsView, not a new tab

- **Status**: accepted
- **Context**: Phase 5's backend read model (`progress::snapshot`) and IPC commands already existed but were never rendered; the PRD's UX requirements ask for derived state "beside the original Markdown checklist," not a separate surface.
- **Consequences**: `EpicsPanel.tsx` (per-project) gained per-loop execution/delivery badges, a discrepancy warning icon, PRD/epic progress bars, an unmatched-records panel, and an export-summary action; `EpicsView.tsx` (cross-project) now prefers the derived epic count over checkbox counting when a project has migrated. No new route or tab; legacy/empty-mode projects transparently fall back to checkbox-derived counts since `execution_file_present` gates every derived value.

## 2026-07-27 — Marked `ResponseAccumulator` cap backlog item done without code change

- **Status**: accepted
- **Context**: `loops.md` P2 line 217 asked to "cap unbounded accumulation in `ResponseAccumulator` (`agents.rs:603-625`) — abort past a block/byte limit." Reading `agents.rs` showed the struct (now at ~748-781, not 603-625 — that range is `task_status_from` after intervening edits) already has `bytes`/`over_limit` fields bounded by `limits::ACCUMULATOR_MAX_BLOCKS`/`ACCUMULATOR_MAX_BYTES`, an `ACCUMULATOR_TRUNCATION_MARKER` stamped on `finish`/`clone_partial`, and two passing tests (`accumulator_caps_blocks_and_marks_truncation`, `accumulator_clone_partial_stamps_truncation_when_over_limit`). This was already delivered by the 2026-07-23 trust-boundary-hardening pass (checked at `loops.md` line 160, "Bound untrusted work") — the P2 line was a stale duplicate never checked off.
- **Consequences**: No source change. `loops.md` line 217 marked `[x]` with a note explaining the stale line-range reference, so the backlog no longer implies open work here.

## 2026-07-27 — Reaffirm Option A: `loopdeck-orchestrator` stays; `prd-skill-split.md`'s unchecked boxes are intentional

- **Status**: accepted
- **Context**: `prd-skill-split.md`'s Phase 2 checkboxes are all unchecked, including its P0 goal "`loopdeck-orchestrator` removed from the embedded skill set." Read cold, that looks like unfinished work, and was briefly (and incorrectly) treated as such: `skills.rs` was edited to drop the orchestrator const/match-arm/`determine_skills` insert, a removal migration was added, `commands/agent.rs::build_next_loop_prompt`'s two `loopdeck-orchestrator` string literals were changed to `loopdeck-loop-runner`, `templates/skills/loopdeck-orchestrator/SKILL.md` was deleted, and this repo's own installed copy + manifest entry were removed. This directly contradicts the 2026-07-25 "0.2.0 skill split + epic-author landed (Option A)" decision, which explicitly chose to *keep* a slimmed orchestrator owning the spawn→stitch→verify→ship flow, and whose own follow-up #2 already documented that the PRD's checkboxes were deliberately left unchecked because Option A deviates from the literal PRD text — not because the removal was still pending.
- **Consequences**: Fully reverted before anything was committed: `git checkout -- src-tauri/src/skills.rs src-tauri/src/commands/agent.rs src-tauri/src/project.rs docs/epics/support-project-management/prd-skill-split.md` (all four were tracked, so this was a clean restore); `templates/skills/loopdeck-orchestrator/SKILL.md` was tracked too and restored the same way. `.claude/skills/` is gitignored, so its dangling `loopdeck-orchestrator` symlink and the `.loopdeck-manifest.json` entry were restored by hand. Test suite back to the 447-passed baseline. `prd-skill-split.md`'s Phase 2 boxes stay unchecked — they are a correct reflection of "not done because the plan changed," not a to-do. If the orchestrator's removal is ever actually wanted, it needs a fresh PRD (or an explicit amendment to this one) that supersedes Option A, not a checkbox pass against the old text.

