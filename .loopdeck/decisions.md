# Decisions

_Older decisions archived to [decisions-archive.md](./decisions-archive.md)._

## 2026-08-01 — Composer image attachments: inline base64, Claude-only, native-drop

- **Status**: accepted
- **Context**: The Agent composer was text-only. Two shapes were possible: write pasted images to `.loopdeck/attachments/` and inject the path so the agent `Read`s them back, or send real Anthropic `image` content blocks. The second was chosen as the intended form. The enabling unknown was verified empirically before any plumbing: `claude 2.1.220` with `--input-format stream-json` accepts the Messages API block shape **verbatim** on stdin (`{"type":"image","source":{"type":"base64",…}}`) — no CLI-specific wrapper — confirmed by a live one-turn run that correctly described the image.
- **Consequences**:
  - **Storage is inline base64** on `ConversationTurn.attachments` (user's explicit choice over a path-plus-sidecar-file layout): transcript lines stay self-contained, nothing to garbage-collect on archive, no broken thumbnails if the project moves. The cost is transcript size, mitigated by downscaling in the browser to a 1568px long edge (the largest dimension the vision model actually uses) before encoding — a typical screenshot lands ~200–400KB instead of 2–3MB. **Watch item**: `limits::TRANSCRIPT_MAX_BYTES` is 16MiB, which image-heavy conversations will now reach far sooner than text-only ones.
  - **Budgets are enforced at the IPC boundary**, not trusted from the frontend (`validate_attachments`): per-image and per-turn base64 caps, a count cap, an allow-list of the four media types the API accepts, and a bare-base64 check — an embedded newline would split the single-line NDJSON turn the CLI reads.
  - **Claude-only for now.** `harness.rs` rejects non-empty attachments for Codex rather than dropping them silently, mirroring the existing `plan_mode` precedent: a user who pastes a screenshot and asks "what's wrong here?" must not get a confident answer about an image the model never received. Codex's `turn/start` takes its own input-item shape; teaching it is deferred behind the separate open bug where a Codex session shuts down after a turn.
  - **Drag-and-drop uses Tauri's native drop events, not HTML5 drops.** Getting bytes in the webview would require `dragDropEnabled: false`, which would break `ImportFlow.tsx`'s folder-drop import (a folder has no `File` equivalent to fall back on). Instead the drop delivers a path and the new `agent_read_image_attachment` command reads it, after which the frontend re-runs the bytes through the same downscale pipeline paste uses. The two listeners never coexist — ImportFlow is its own route.
  - A turn with images and no text omits the text block entirely rather than sending an empty one (the API rejects a blank block), so "look at this" with no words is a valid message.

## 2026-07-27 — Amended `prd-skill-split.md` in place to document Option A (no code change)

- **Status**: accepted
- **Context**: Both the 2026-07-25 and 2026-07-27 entries above explain Option A only in `decisions.md` — the PRD file itself still read as if literal removal were the plan, with all Phase 2/3 boxes unchecked and no in-file explanation. That's exactly the setup that produced the 2026-07-27 near-miss (an agent reading the unchecked boxes cold, without first grepping `decisions.md`, and briefly re-implementing the deletion). The 2026-07-27 entry's own closing line named the fix: "an explicit amendment to this one," not another checkbox pass.
- **Consequences**: `prd-skill-split.md` now carries the context in-file: frontmatter `status` changed `proposed` → `accepted`; a new "Amendment — Option A shipped instead of literal removal" section up top explains what shipped vs. the original text, cross-references both dated decisions, and explicitly warns against "finishing" the removal items without a fresh reversing decision. The Goals table's two orchestrator-removal rows are struck through and marked **Superseded (Option A)** with a third column recording what actually shipped; every other Goals row and Phase 1–3 checklist item is checked off against a real file/symbol (`skills.rs:577` `test_core_skills_always_included`, `commands/project.rs::refresh_skills`, `ProjectDetail.tsx`, both new-skill SKILL.md files). `skill-split/remove-orchestrator-const` and `skill-split/orchestrator-removal-migration` stay unchecked with an explicit "superseded, not a to-do" note instead of bare `[ ]`. Two items were found genuinely still open (not deviation, just unfinished) and left unchecked as real backlog: `skill-split/prefix-convention-doc` (no `CONTRIBUTING.md` exists anywhere in this repo) and `skill-split/dogfood-refresh` (never exercised directly against this repo's own gitignored, currently-absent `.claude/skills/`). No source change; doc-only.

## 2026-07-27 — Reconciled the two 0.3.0 `agent-full-access` PRDs before starting 0.4.0

- **Status**: accepted
- **Context**: Kicking off the 0.4.0 `overnight-orchestration` epic surfaced that both 0.3.0 PRDs (`prd-full-access-tier.md`, `prd-verify-and-ship-skills.md`) still showed 0 checked boxes each, even though the functionality they describe had genuinely shipped — the same stale-checkbox pattern already reconciled twice for `support-project-management` (`prd-skill-split.md`, `prd-structured-execution-state.md`). Two different causes this time: (1) `prd-full-access-tier.md`'s runtime work landed 2026-07-23 as `PermissionMode::Autonomous`/`ProjectEntry::autonomous: bool` — a leaner, differently-named surface than the PRD's literal `FullAccess` enum design (`intercepts_manually()`, `ProjectPermissionMode`, toolbar selector) — so a literal diff against the PRD's own symbol names would have found nothing and looked unstarted. (2) `prd-verify-and-ship-skills.md`'s skills work landed 2026-07-23/24 but was tracked under `loops.md`'s "Release Gate C" heading, not against this PRD file — Gate C predates the epic/PRD-file convention introduced for 0.2.0, so nobody was in the habit of checking these particular boxes yet.
- **Consequences**: Both files amended in place (frontmatter `status: proposed` → `accepted`; an "Amendment" section explaining what shipped vs. what the Design section specifies, with file:line citations). `prd-full-access-tier.md`: every Phase 1 item and most of Phase 2/3 checked against real code (`permission.rs:93,123`, `config.rs:161`, `commands/project.rs:290`, `claude_session.rs:659,768`, `codex_session.rs` parity tests, `ProjectDetail.tsx:454-516` for the relocated toggle). Two Phase 2 items left unchecked (AgentPanel/AgentRunner *toolbar* selector — the toggle lives in ProjectDetail's Overview section instead, a deliberate relocation, not unfinished) and two Phase 3 items left unchecked as genuinely open (no dedicated `ProjectPermissionMode`-style round-trip test since the field is a plain bool; `set_project_autonomous` silently no-ops on an unknown path instead of erroring, so the PRD's error-path test has nothing to test until that's decided). `prd-verify-and-ship-skills.md`: all of Phase 1–4 checked — the skills at `templates/skills/loopdeck-prd-verifier/` and `templates/skills/loopdeck-open-pr/` (relocated from `.agents/skills/` by the 2026-07-25 SSOT reconciliation) match the Design section line-for-line, and the orchestrator wiring (`templates/skills/loopdeck-orchestrator/SKILL.md`) has Phases 6-7, the updated ASCII diagram, and no stale "Phase 6"/"Decide Next Phase" references (grep-confirmed). Left unchecked: the per-stack smoke tests (Go/Node/unknown-marker throwaway repos) — only this repo's own Rust+Node markers have ever been exercised live. No source code changed; doc-only reconciliation, done ahead of `prd-safety-prereqs` (0.4.0) so the epic history the new epic's README references is actually accurate.

## 2026-07-27 — Landed `prd-safety-prereqs` Phase 1 (floor hardening) + Phase 2 (CI matrix)

- **Status**: accepted
- **Context**: `docs/epics/overnight-orchestration/prd-safety-prereqs.md` (ADR-6) gates the rest of the 0.4.0 epic — no unattended run should exist before the destructive floor's `mv`/`cp`/`rsync` gap closes and CI covers more than one OS. Investigating Phase 2 found `.github/workflows/ci.yml` already existed (Release Gate A, 2026-07-23) running fmt/clippy/test/build — just on a single `macos-latest` runner, not the macOS+Ubuntu matrix this PRD and the stale `loops.md` P3 backlog line both asked for.
- **Consequences**: **Phase 1** — `permission.rs` gained `destructive_move_target`/`expand_destination`/`lexically_normalize`/`home_dir` and a new `mv`/`gmv`/`cp`/`gcp`/`rsync` arm in `analyze_stage`: the last argv token (destination) is expanded (`~`, `~/…`, `$HOME`, `${HOME}`) and lexically normalized (`..`/`.` collapsed, no filesystem access since the destination need not exist), then hard-denied on an **exact** match against `/`, `/etc`, `/usr`, `/var`, or the resolved home directory. Exact-match (not prefix) means subpaths — `/var/tmp/x`, `/etc/myapp.conf`, `~/Downloads` — are untouched, which resolves the PRD's `/var/tmp`/`/var/folders` open question for free (documented in the PRD in place, no code special-casing needed). A purely relative destination with no absolute/`~`/`$HOME` anchor (e.g. `../etc` against an unknown cwd) is intentionally out of scope — this floor has no cwd to resolve against and stays a syntactic deny-list, not a sandbox. 6 new tests (`permission.rs`), all injecting a fabricated home directory into the pure helper functions rather than mutating the process-global `HOME` env var (which would race other tests in the same binary). The `claude_session.rs:218-224` doc-comment item in the same PRD phase turned out to be stale — that drift was already fixed 2026-07-23 as part of the SECURITY.md work; no code change needed, just noted in the PRD. **Phase 2** — converted `ci.yml`'s single-OS job to `strategy.matrix.os: [macos-latest, ubuntu-latest]`, with an Ubuntu-only system-dependency step (`libwebkit2gtk-4.1-dev`, `libappindicator3-dev`, `librsvg2-dev`, `patchelf` — mirrors `build.yml`'s existing Linux leg) ahead of the Rust toolchain, and a per-OS `rust-cache` key so the two legs don't collide. Added a CI badge to `README.md`. No separate `tsc --noEmit` job — `npm run build` already runs `tsc` first. **Not done this session (Phase 3, "Prove the gates"):** no throwaway PR was pushed to observe CI actually fail on a deliberate lint error, and no live Autonomous-mode session was run to eyeball an `mv`/`cp` floor denial in the audit log — the log-path guarantee is structural (every `Decision::Deny` flows through the same unconditional `tracing::info!` call, floor rule or not) but wasn't observed live. Both left unchecked in the PRD as the remaining manual step. Verified: `cargo fmt --check`, `cargo clippy --lib -- -D warnings`, `cargo test --lib` (451 passed, 8 ignored), `npx tsc --noEmit`, `npm run build` all green.

## 2026-07-28 — Landed `prd-safety-prereqs` Phase 3 (Prove the gates, live) — PRD now fully done

- **Status**: accepted
- **Context**: The two Phase 3 steps left open by the 2026-07-27 decision above both require a live, externally-observable action rather than more code: pushing a real PR to `suprie/loopdeck` to watch CI actually go red, and observing a real `tracing::info!` log line rather than just citing the unconditional call site. Both are "visible to others" / "runs live code" actions per the session's care-with-actions posture, so confirmed with the user via `AskUserQuestion` before doing either (recommended options: push+close a real throwaway PR; run a local Rust smoke test rather than a full GUI Autonomous session for the audit-log check).
- **Consequences**: **Step 1 (throwaway PR)** — branch `throwaway/ci-gate-smoke-test` off `main`, one commit appending a deliberately-bad `throwaway_ci_smoke_check` fn to `lib.rs` (`if flag == true { return true; } return false;`), pre-verified locally to fail `cargo clippy --lib -- -D warnings` on exactly `clippy::bool_comparison` + `clippy::needless_return` before pushing. Pushed, opened PR #27 (`gh pr create`), polled `gh pr checks 27` in a background loop until non-pending: both `fmt · clippy · test · build (macos-latest)` and `(ubuntu-latest)` reported `fail`. Closed the PR with a comment recording the CI result and deleted the branch in one call (`gh pr close 27 --comment … --delete-branch`) — never merged, working tree was clean before and after (confirmed via `git status --porcelain`). **Step 2 (audit-log spot-check)** — a full GUI Autonomous-mode run wasn't available in this environment, so the equivalent proof was done at the code level: a temporary `#[test]` added to `permission.rs`'s existing test module (not a new file) constructs `PermissionPolicy::with_mode(PermissionMode::Autonomous)`, calls `.decide("Bash", {"command": "mv report.txt /var"})` (the same absolute-destination form already covered by the Phase 1 tests, so no new floor behavior was being tested — only whether the *logging* fires), and emits the **verbatim** `tracing::info!` macro invocation from `claude_session.rs:682-688` (identical field list and message) rather than a paraphrase, so passing this test is direct evidence the production log call would fire, not an inference about it. `cargo test -- --nocapture` printed: `behavior="deny" reason="mv destination resolves to protected root \`/var\` blocked by policy floor" autonomous=false`. The test was then reverted (`git checkout -- src-tauri/src/permission.rs`) — it was a one-time observation, not a permanent addition, since the floor logic itself is already covered by Phase 1's 6 tests and this only needed to confirm the *log line*, once, live. `prd-safety-prereqs` is now fully done across all 3 phases — ADR-6's gate on the rest of the 0.4.0 epic is satisfied, so `prd-run-queue` can start.

## 2026-07-28 — `prd-run-queue` Phase 1: RunPlan is data-only, no OCC/backup; budgets are recorded not enforced

- **Status**: accepted
- **Context**: `prd-run-queue.md` Phase 1 asks for `RunPlan`/`RunPhase`/`StallPolicy`/`RunBudgets` structs with serde defaults and atomic persistence to `.loopdeck/run-plan.yaml`, referencing phases by stable execution ID. The closest precedent, `execution.rs` (0.2.1), additionally carries an optimistic-concurrency `revision` field and `.bak`-backed non-destructive recovery — but that machinery exists specifically because `execution.yaml` has multiple concurrent writers (the `loopdeck state` CLI, IPC commands, and the migration path). A run plan has exactly one writer: the in-app executor that will exist from Phase 2 onward. Also, the PRD's own Non-Goals explicitly separate this PRD's job (sequencing and state) from `prd-unattended-ship`'s job (budget *enforcement*) — Phase 1 only needed a place to record the queue-time budget values, not a spend tracker.
- **Consequences**: `src-tauri/src/runplan.rs` intentionally omits `execution.rs`'s revision/OCC and `.bak` recovery — `load`/`save` are a plain bounded-read-and-parse / atomic-write pair, no `expected_revision` parameter, no backup file. If Phase 2's executor turns out to need concurrent writers after all (e.g. a UI cancel racing an executor status update), add the OCC field then — no data already on disk breaks, since `revision` would arrive as a new `#[serde(default)]` field like `execution.rs`'s did. `RunBudgets` fields (`per_phase_token_cap`, `per_phase_wall_clock_secs`, `total_run_wall_clock_secs`) are all `Option<u64>`, documented as "`None` means use `prd-unattended-ship`'s named-constant default, not unbounded" — this module never reads or enforces them, only stores what the picker UI (Phase 5) will write. Mirrors `execution.rs`'s Phase 2→3 pattern: a module-level `#![allow(dead_code)]` covers the currently-uncalled `pub` surface until Phase 2's executor + IPC commands become the first production callers; remove the allow then, not before.
## 2026-07-28 — Archived decisions.md to the 15-live-entry cap; re-synced .claude/skills to templates SSOT
- **Status**: accepted
- **Context**: `decisions.md` had grown to 90 entries / 812 lines (~74K tokens) with no active cap, costing a full read on every append (Edit requires a prior Read) and every context lookup — the exact cap/archive convention already existed in `templates/skills/loopdeck-memory/SKILL.md` (15-live-entry cap, `decisions-archive.md` overflow, terse 3-bullet format) but this worktree's `.claude/skills/` held stale pre-cap copies instead of the documented symlink-to-`templates/` mirror (the 2026-07-24 SSOT decision), so the fix was never live.
- **Consequences**: Archived the oldest 75 entries to new `.loopdeck/decisions-archive.md` (pointer left at the top of `decisions.md`); replaced all 5 present `.claude/skills/<name>` real-directory copies (`loopdeck-memory`, `loopdeck-loop-runner`, `loopdeck-epic-author` were stale; `loopdeck-open-pr`/`loopdeck-prd-verifier` already matched) with symlinks to `../../templates/skills/<name>`, restoring the documented SSOT. `loops.md`'s own bloated `## Current`/History (and the empty, seemingly-unused `execution.yaml` alongside a still-present `loops.legacy.md`) were flagged but not touched — out of scope for this pass.

## 2026-07-28 — `prd-run-queue` Phase 2: executor reuses `start_fresh_and_record`; non-green verdict halts the run, doesn't skip ahead
- **Status**: accepted
- **Context**: The PRD's Design section left the executor's session-spawn mechanics and its relationship to `commands::agent`'s existing turn pipeline as an open implementation question, and left non-green-verdict handling (continue vs. stop) unspecified pending Phase 4's stall-policy runtime.
- **Consequences**: `commands/run_queue.rs::execute_run` drives each queued phase through `commands::agent::start_fresh_and_record` (widened to `pub(crate)`) — the same spawn/retry/transcript pipeline a human "Start Loop" click uses — rather than a parallel implementation, so `retry.rs` backoff came for free. A non-green verdict (WARN/BLOCK/none) or turn error marks the phase `Failed` and stops the run entirely rather than skipping to the next phase, since without Phase 4's stall-vs-failure distinction a later phase could build on a dependency that never actually finished; `depends_on` is recorded but not yet consulted (Phase 2 runs the plan's authored vec order).
- **Detail**: `.loopdeck/loops.md` `## Current` (2026-07-28 `prd-run-queue` Phase 2 entry) has the full file/symbol breakdown.

## 2026-07-28 — Two independent sessions built the same `prd-run-queue` Phase 2; kept #29, closed #30, fixed one bug it left behind
- **Status**: accepted
- **Context**: A separate parallel session (this same repo, worktree `worktree-prd-run-queue-phase2`) and this session both independently implemented `prd-run-queue` Phase 2 from cold, unaware of each other — same PRD, same file list (`run_executor.rs`, `commands/run_queue.rs`). #29 merged to `main` first; this session's PR #30 (branched before #29 merged) then failed CI on genuine symbol conflicts against the now-updated `main`, surfacing the collision.
- **Consequences**: Compared both implementations line-by-line rather than blindly rebasing. #29 was stronger — truly async `queue_run` via `tokio::spawn`/`AppHandle` (doesn't block the IPC call for the run's full duration), correctly clears `execution.yaml`'s `current` via `abandon_current` on a failed/non-PASS phase, extra pre-flight guards (no queued phases / a loop already current), and a prompt that tells the agent it's an unattended run and to respect `RunConsent.draft_pr_authorized`. #30 had a real bug (never cleared `current` on failure, permanently blocking future promotes) and a blocking `queue_run`. Closed #30 unmerged with the comparison recorded in the closing comment; no code from #30 was merged. Separately found and fixed (PR #31) that #29's `cancel_run` doc comment overclaimed a mid-turn interrupt — `fire_interrupt` fires `interrupt_slots`, but the executor's `start_fresh_and_record` → `send_message` path discards its `InterruptSlot` param, so cancellation only takes effect between phases, same as #30's version; doc-only fix, no behavior change.
- **Detail**: Two sessions on the same repo/PRD without either aware of the other is a coordination gap worth naming for next time — worth checking `git log origin/main` / open PRs for the target PRD before starting a loop already "in progress," not just `.loopdeck/loops.md`'s own `## Current`, which can lag a concurrent session's merge.

## 2026-07-28 — `prd-run-queue` Phase 3: interview answers extracted via a greppable text marker, not raw tool-call plumbing

- **Status**: accepted
- **Context**: The PRD's Design section says the pre-flight interview reuses "the existing question-card IPC surface" — a bounded session whose `AskUserQuestion` calls render as the same cards chat already shows. `start_fresh_and_record` already parks and resumes on those cards internally (via `ParkSlots`/`QuestionSlot`), but it returns only the turn's final `AgentResponse`, not the raw `QuestionAnswers` map exchanged mid-turn — plumbing that map out would mean touching `claude_session.rs`'s park/resume internals for a one-off consumer.
- **Consequences**: `run_executor::build_interview_prompt` instructs the agent to close its final message with a `## Pre-flight Answers` block restating each Q/A pair verbatim; `extract_interview_answers` parses that block with the same last-occurrence-wins rule `extract_verdict` already established for verify verdicts (a turn's own reasoning may quote the format while explaining it — the real block is last). This reuses an already-proven pattern instead of adding a second way to extract structured data from a turn. Trade-off: the parsed answers are only as faithful as the agent's own restatement, not a verbatim capture of the underlying `QuestionAnswers` — acceptable here since the same trust boundary already applies to every other agent-authored artifact this codebase reads back (verify verdicts, commit messages, PR bodies).
- **Detail**: New `RunPhase.interview_status: InterviewStatus` (`Pending`/`Answered`/`Skipped`) is the actual gate `queue_run` checks — `interview: Vec<PinnedAnswer>` alone couldn't distinguish "not yet interviewed" from "interviewed, nothing was ambiguous" (both are an empty list). `skip_phase_interview` sets `Skipped` without running a session. See `.loopdeck/loops.md` `## Current` for the full file/symbol breakdown.

## 2026-07-28 — "Start next loop" skips `Review & merge:` reminders instead of "implementing" them

- **Status**: accepted
- **Context**: `loopdeck-open-pr`'s Phase 7 appends `- [ ] Review & merge: <url>` to `.loopdeck/loops.md`'s `## Next Steps` as a human bookkeeping reminder. `next_unchecked_loop_step` (`commands/agent.rs`) treated every `- [ ]` line in that section as equally actionable, so after a user merged a PR and pulled, "Start next loop" picked the still-unchecked merge reminder and told the agent to "implement" it — the agent's only compliant move was to check the box, which read to the user as the app silently marking a real task done instead of starting new work.
- **Consequences**: `next_unchecked_loop_step` now skips any `- [ ] Review & merge:` line (case-insensitive prefix match) and keeps scanning for the next real step; if only reminders remain it falls through to the existing "propose the next loop" prompt instead of fabricating work. Fix is scoped to the one function with the false positive — did not touch `loopdeck-open-pr`'s insertion behavior, since the reminder itself is still useful for a human. Flagged as a background task: the distinction is still a hardcoded string match rather than part of the `loops.md` format itself, so a future skill adding a different non-actionable reminder type would reproduce the same bug class.

## 2026-07-28 — `prd-run-queue` Phase 4: stalls only detectable via streaming + `TURN_DEADLINE`, not interrupted early

- **Status**: accepted
- **Context**: Detecting a mid-run `AskUserQuestion`/permission/plan stall requires the executor's phase turns to run through the streaming pipeline (the non-streaming path auto-denies these cards instead of parking), and `claude_session.rs`'s park site can't be cancelled early — it isn't selected against any interrupt once entered, only against its own 30-minute `TURN_DEADLINE` backstop.
- **Consequences**: `execute_run` now sends each phase via `start_fresh_and_record_streaming` with a no-op sink channel and catches the new `AppError::TurnParked` (fired at `TURN_DEADLINE`) to mark the phase `Parked` instead of `Failed`; a pure `run_executor::phases_blocked_by_park` then applies the plan's `StallPolicy` to the remaining `Queued` phases. A stall is unavoidably a ~30-minute-worst-case cost per phase in this codebase's current single-session-per-project architecture — shortening it needs a session-model change out of this PRD's sequencing/state scope.
- **Detail**: `.loopdeck/loops.md` `## Current` (2026-07-28 `prd-run-queue` Phase 4 entry) has the full file/symbol breakdown.

## 2026-07-29 — Fix unscrollable AskUserQuestionCard in ProjectDetail's stuck-prompt callout
- **Status**: accepted
- **Context**: `Chat.tsx` already wraps `AskUserQuestionCard` in `max-h-[45vh] overflow-y-auto` so a question with many options/questions stays reachable. `ProjectDetail.tsx`'s `StuckQuestionCallout` (the tab-agnostic banner) mounted the same card with no scroll constraint, so 5+ questions overflowed the callout with no way to reach the Submit button.
- **Consequences**: Wrapped the card in the same `max-h-[45vh] overflow-y-auto` div as `Chat.tsx`, matching the existing pattern instead of inventing a new one.

## 2026-07-29 — Wired `RunConsent.draft_pr_authorized` through to a real unattended-draft-PR bypass

- **Status**: accepted
- **Context**: `RunConsent.draft_pr_authorized` (`runplan.rs`) has existed since Phase 1 with a doc comment claiming it "authorizes `gh pr create --draft` with no interactive confirmation" — but it was dead: never read by `run_executor`/`run_queue`, and `loopdeck-open-pr`'s Phase 4 confirm gate had zero conditional/bypass language, so a queued overnight run could never actually open a PR without a human present to answer "proceed, edit, or abort?".
- **Consequences**: `build_phase_prompt` (`run_executor.rs`) now takes `draft_pr_authorized: bool`, threaded from `plan.consent.draft_pr_authorized` at its one call site (`commands/run_queue.rs::execute_run`); when true it tells the agent, in plain text, that this run is pre-authorized to skip `loopdeck-open-pr`'s Phase 4 confirmation and open Phase 6 as `gh pr create --draft` (no `--web`) — the `false` branch reproduces the prior prompt text byte-for-byte, so non-consented runs are unaffected. `loopdeck-open-pr`'s SKILL.md (and its byte-identical mirror in `templates/skills/loopdeck-open-pr/`) gained a new Phase 4a that checks for that exact stated authorization before the (unchanged) Phase 4b confirmation gate, plus a `--draft`/no-`--web` Phase 6a variant — the confirm gate stays the unconditional default for every interactively-invoked run; only a run whose prompt explicitly states pre-authorization can skip it. Draft-only is enforced in both the prompt text and the skill doc: nothing in this change ever readies or merges a PR automatically.
- **Detail**: The interactive "Start next loop" path (`commands/agent.rs`) is untouched — it always confirms, by design, matching the session's own publish-safety rules. The Phase 5 picker UI that would let a human actually set `draft_pr_authorized=true` from the app doesn't exist yet (out of scope here); this only finishes the executor↔skill wiring for when that UI lands. See PR #33 (same session) for the complementary fix: `next_unchecked_loop_step` now skips `- [ ] Review & merge:` bookkeeping lines instead of "implementing" them — relevant because Phase 7 still appends that same reminder line after a draft PR opens, and a human still has to act on it manually.

## 2026-08-01 — `create_run_plan`: the missing write path a picker UI needs to exist at all

- **Status**: accepted
- **Context**: Implementing `prd-run-queue` Phase 5 (phase picker UI) surfaced a gap the PRD's Design section hadn't named: `queue_run`/`run_phase_interview`/`skip_phase_interview` all *read* `.loopdeck/run-plan.yaml` via `runplan::load`, but nothing in Phases 1-4 ever *wrote* a fresh plan from a set of selected phases — Phase 1's tests construct a `RunPlan` directly in Rust, and there was no IPC path for a picker selection to become one.
- **Consequences**: New pure `run_executor::build_run_plan(id, project, created, execution_ids, stall_policy, draft_pr_authorized) -> RunPlan` builds the phase list with `depends_on` defaulting to the authored/selection order (each phase depends only on its immediate predecessor), matching the PRD's "linear chain, no editor" v1 lean — kept pure and unit-tested (no `AppState`, no disk I/O) per this module's existing split between domain logic here and `AppState`-dependent glue in `commands/run_queue.rs`. The new `create_run_plan` command validates every ID against `epic::find_loop_by_id` before writing (a stale/renamed ID is rejected at queue time, not discovered mid-run), refuses while a run is already in progress (the same `run_handles` guard `queue_run` already uses), and otherwise replaces any existing plan outright — a fresh picker selection is always meant to start a new plan, not merge into a stale one from a previous night.
- **Detail**: `.loopdeck/loops.md` `## Current` (2026-08-01 `prd-run-queue` Phase 5 entry) has the full frontend breakdown, including why the Interview UI checklist item needed no new question-card plumbing (it reuses `ProjectDetail.tsx`'s existing tab-agnostic `StuckQuestionCallout`).

  **Correction (2026-08-01, same day):** this entry's "no new question-card plumbing" claim was wrong — see the decision immediately below.

## 2026-08-01 — Closed a duplicate `prd-run-queue` Phase 5 PR, kept #38, fixed the `AskUserQuestion` bug both implementations shared

- **Status**: accepted
- **Context**: A separate session (this same repo) independently implemented `prd-run-queue` Phase 5 from cold in this session, unaware of a parallel session doing the same — same PRD, same file list, even the same new `RunQueuePanel.tsx` filename. The parallel session's PR #38 merged to `main` first; this session's PR #39 (branched before #38 merged) then failed CI with duplicate-symbol errors (`create_run_plan` defined twice) once GitHub tested it against the now-updated `main` — the same collision shape as the 2026-07-28 Phase 2 duplicate (`#29`/`#30`).
- **Consequences**: Compared both implementations line-by-line rather than blindly closing either. #38 was stronger: it exposed `stall_policy` and `draft_pr_authorized` as real picker controls (`create_run_plan(path, execution_ids, stall_policy, draft_pr_authorized)`) — closing a gap the 2026-07-29 `RunConsent.draft_pr_authorized` decision had explicitly flagged as still missing — and extracted a pure, unit-tested `run_executor::build_run_plan` helper, where this session's version built the same `RunPhase` list inline with no test (citing "no mockable `AppState` seam," which didn't actually apply to the pure construction logic). Closed PR #39 unmerged with the comparison recorded in the closing comment; no code from #39 was merged. Separately found and fixed (this fix, on a fresh branch off the updated `main`) that **both** implementations shared a real bug: `run_phase_interview` called the non-streaming `start_fresh_and_record`, and `claude_session.rs::answer_ask_user_question`'s own doc comment says a non-streaming (`channel: None`) `AskUserQuestion` has no UI surface and is auto-denied immediately instead of parked — so the interview's own clarifying questions (the entire point of `build_interview_prompt`, which explicitly instructs the agent to call `AskUserQuestion` and wait for the answer) could never actually reach a human. Both PRs' changelogs (and PRD checklist) had independently made the same incorrect claim that this already worked "the same as any other turn."
- **Detail**: Fix: `run_phase_interview` now calls `start_fresh_and_record_streaming` with a no-op sink `Channel` — the same pattern Phase 4's executor established for `execute_run`. A channel's mere *presence* (not what its callback does with events) is what keeps `answer_ask_user_question` on the parking path instead of the deny-immediately branch; the parked card then surfaces through the pre-existing tab-agnostic `StuckQuestionCallout` (`ProjectDetail.tsx`), so no new question-card component was needed — only the pipeline choice underneath it. Corrected the false claim in both `prd-run-queue.md`'s Phase 5 checklist and `loops.md`'s Phase 5 changelog entry, cross-referencing this decision. Gates green: fmt/clippy(lib, `-D warnings`)/test(484 passed, 8 ignored). No frontend change (the bug was purely a backend pipeline choice).
