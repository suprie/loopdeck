# Session Summary — 2026-06-27
## What we learned about Claude Code internals (from packet sniffing):

System prompt contains: CLAUDE.md + available skill registry (names + trigger descriptions) + agent metadata
User message has 3 blocks: <system-reminder> (CLAUDE.md + project context), command invocation, skill content with cache_control: ephemeral
Skill resolution is a 2-step round-trip: first request resolves which skill to use, second injects skill content and executes
--resume <session_id> restores full conversation history (verified manually)

## LoopDeck direct API path (future):

*Input*: --input-format stream-json — proper programmatic interface, underdocumented, worth experimenting
Optimization opportunity: pre-select skill yourself → skip the skill resolution round-trip (save 1 API call)
Further optimization: replace verbose system prompt with shorter version since you own the routing logic

## Session persistence:

Store *session_id* in _.loopdeck/session_id_ per project
*--resume* for short-term continuity, loop files for long-term structured context — complementary layers

## Parking lot:

RTK integration (token savings on tool output layer, complementary to LoopDeck)

## Next action:

Experiment with --input-format stream-json, reference claude-agent-sdk-go/docs/cli-protocol.md for wire format
